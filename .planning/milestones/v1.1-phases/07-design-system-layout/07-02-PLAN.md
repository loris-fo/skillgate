---
phase: 07-design-system-layout
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/src/components/header.tsx
  - apps/web/src/components/footer.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/report/[slug]/page.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03]

must_haves:
  truths:
    - "All pages share a header with skillgate wordmark linking home"
    - "Header has a copyable npm install pill"
    - "Header has a GitHub icon link"
    - "Header is sticky with frosted-glass blur on scroll"
    - "All pages share a footer with GitHub, npm, skillgate.sh, and MIT License links"
    - "Footer links are in muted #94A3B8 text"
    - "Footer is pushed to viewport bottom via min-h-screen flex layout"
  artifacts:
    - path: "apps/web/src/components/header.tsx"
      provides: "Shared site header"
      exports: ["Header"]
      min_lines: 30
    - path: "apps/web/src/components/footer.tsx"
      provides: "Shared site footer"
      exports: ["Footer"]
      min_lines: 15
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root layout wrapping children with Header and Footer"
      contains: "Header"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/header.tsx"
      via: "import and render"
      pattern: "import.*Header.*header"
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/footer.tsx"
      via: "import and render"
      pattern: "import.*Footer.*footer"
    - from: "apps/web/src/components/header.tsx"
      to: "clipboard API"
      via: "onClick copy for npm pill"
      pattern: "navigator.clipboard"
---

<objective>
Create shared Header and Footer components and wire them into the root layout so every page gets consistent navigation and branding.

Purpose: Every page automatically renders inside the shared layout with the skillgate wordmark, npm install pill, GitHub link in the header, and footer links.
Output: header.tsx, footer.tsx components, updated layout.tsx, adjusted page files.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-system-layout/07-CONTEXT.md
@.planning/phases/07-design-system-layout/07-01-SUMMARY.md
@apps/web/src/app/layout.tsx
@apps/web/src/app/page.tsx
@apps/web/src/app/report/[slug]/page.tsx
@apps/web/src/components/copy-button.tsx
</context>

<interfaces>
<!-- Existing CopyButton component that can be referenced for clipboard pattern -->
From apps/web/src/components/copy-button.tsx:
```typescript
export function CopyButton({ text, label, className }: {
  text: string;
  label: string;
  className?: string;
}): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Header and Footer components</name>
  <files>apps/web/src/components/header.tsx, apps/web/src/components/footer.tsx</files>
  <action>
Create `apps/web/src/components/header.tsx` as a client component ("use client"):

Header requirements (from CONTEXT.md locked decisions):
- Sticky positioning: `sticky top-0 z-50`
- Frosted-glass blur: `backdrop-blur-md bg-bg-page/80` (or use the surface-0 alias with alpha)
- Full viewport width, inner content constrained with `max-w-6xl mx-auto px-4 sm:px-6`
- Height: use `py-4` for comfortable spacing
- Flex row with items-center, justify-between

Left side:
- "skillgate" text as Next.js `<Link href="/">` with classes: `font-sans font-bold text-lg text-text-heading tracking-tight` (Plus Jakarta Sans bold, #0C1A1A)

Right side (flex row, items-center, gap-3):
- npm install pill: A `<button>` showing `npm i -g skillgate` in mono font (`font-mono text-sm`), with `bg-[#BAE6FD]/40 border border-border-card rounded-full px-3 py-1 text-text-body` styling. On click: copy `npm i -g skillgate` to clipboard and show "Copied!" briefly (use useState + setTimeout pattern like CopyButton). Use `navigator.clipboard.writeText()`.
- GitHub icon: An `<a>` linking to `https://github.com/lorisfochesato/skillgate` with `target="_blank" rel="noopener noreferrer"`. Use an inline SVG for the GitHub mark (24x24). Color: `text-text-muted hover:text-accent transition-colors`.

Create `apps/web/src/components/footer.tsx` as a server component (no "use client"):

Footer requirements:
- `py-8` padding, centered content with `max-w-6xl mx-auto px-4 sm:px-6`
- Single row of links separated by `·` (middle dot) character
- All links in `text-sm text-text-muted hover:text-text-body transition-colors`
- Links: GitHub (same repo URL), npm (`https://www.npmjs.com/package/skillgate`), skillgate.sh (`https://skillgate.sh`), "MIT License" (link to repo LICENSE file or just text)
- Center-aligned: `text-center`
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && test -f apps/web/src/components/header.tsx && test -f apps/web/src/components/footer.tsx && grep "skillgate" apps/web/src/components/header.tsx && grep "npm" apps/web/src/components/header.tsx && grep "GitHub" apps/web/src/components/footer.tsx && echo "OK"</automated>
  </verify>
  <done>Header component exports with wordmark link, npm copy pill, GitHub icon. Footer component exports with 4 links in muted text.</done>
</task>

<task type="auto">
  <name>Task 2: Wire layout and adjust existing pages</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/app/report/[slug]/page.tsx</files>
  <action>
Update `apps/web/src/app/layout.tsx`:
- Import Header from `@/components/header` and Footer from `@/components/footer`
- Wrap {children} in a flex column layout for footer-push-down:
  ```
  <body className="bg-bg-page text-text-primary font-sans antialiased min-h-screen flex flex-col">
    <Header />
    <main className="flex-1">
      {children}
    </main>
    <Footer />
  </body>
  ```
- Note: `bg-bg-page` uses the new token from Plan 01. If the alias `bg-surface-0` was kept, either works.

Update `apps/web/src/app/page.tsx`:
- Remove the `<header>` block with the "Skillgate" h1 and tagline (this is now in the shared Header)
- Keep the `<main>` wrapper but remove the outer `<main>` tag since layout.tsx now provides `<main>`. Convert to a `<div>` or `<section>`.
- Keep AuditForm card with its max-w-2xl constraint and card styling
- The page should just be the audit form card content, since the shared layout handles header/footer

Update `apps/web/src/app/report/[slug]/page.tsx`:
- Remove the `<main>` wrapper tag (layout.tsx provides it now). Use a `<div>` or `<section>` with the same `max-w-4xl mx-auto py-8 px-4 animate-fade-in` classes.
- Content stays identical otherwise.

After changes, run `npx turbo build --filter=web` to ensure build passes.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep "Header" apps/web/src/app/layout.tsx && grep "Footer" apps/web/src/app/layout.tsx && grep "flex-1" apps/web/src/app/layout.tsx && npx turbo build --filter=web 2>&1 | tail -5</automated>
  </verify>
  <done>Layout wraps all pages with Header and Footer. Existing pages render without duplicate headers. Build passes. Footer is pushed to bottom of viewport.</done>
</task>

</tasks>

<verification>
1. `cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web` — must succeed
2. `grep -r "Header\|Footer" apps/web/src/app/layout.tsx` — both present
3. `grep -c "<header>" apps/web/src/app/page.tsx` — should be 0 (removed duplicate)
</verification>

<success_criteria>
- Header component renders sticky with frosted-glass blur, contains wordmark link, npm copy pill, GitHub icon
- Footer component renders with 4 links in muted text
- layout.tsx wraps children with Header above and Footer below in flex column
- Footer sits at viewport bottom on short pages (min-h-screen flex layout)
- Existing pages (home, report) render without duplicate headers or broken layouts
- Web app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-layout/07-02-SUMMARY.md`
</output>
