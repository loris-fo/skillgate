---
phase: 08-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/src/components/landing/animated-mockup.tsx
  - apps/web/src/components/landing/badge-snippet.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
autonomous: true
requirements: [LAND-03, LAND-04]

must_haves:
  truths:
    - "Scrolling down reveals a fake audit report inside a browser chrome frame"
    - "Report mockup rows stagger in sequentially as user scrolls into view"
    - "Badge section shows multiple badge variants (safe, warning, critical)"
    - "Generic badge markdown snippet is displayed with a working copy button"
  artifacts:
    - path: "apps/web/src/components/landing/animated-mockup.tsx"
      provides: "Scroll-triggered animated report mockup in browser chrome frame"
      min_lines: 80
    - path: "apps/web/src/components/landing/badge-snippet.tsx"
      provides: "Badge preview with variants and copyable markdown snippet"
      min_lines: 40
  key_links:
    - from: "apps/web/src/app/page.tsx"
      to: "animated-mockup.tsx, badge-snippet.tsx"
      via: "import and render"
      pattern: "import.*landing"
    - from: "badge-snippet.tsx"
      to: "copy-button.tsx"
      via: "import CopyButton"
      pattern: "import.*CopyButton"
    - from: "animated-mockup.tsx"
      to: "Intersection Observer API"
      via: "useEffect with IntersectionObserver"
      pattern: "IntersectionObserver"
---

<objective>
Build the animated report mockup and badge snippet sections for the landing page.

Purpose: Show the product in action (animated mockup) and give skill authors a clear call-to-action (badge snippet).
Output: Two client components wired into the landing page below the feature cards.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-page/08-CONTEXT.md
@.planning/phases/08-landing-page/08-01-SUMMARY.md

<interfaces>
<!-- From globals.css - design tokens -->
Tailwind @theme tokens:
- bg-surface-card (#FFF), border-border-card (#BAE6FD), shadow-card
- text-text-heading, text-text-body, text-text-muted
- bg-accent (#06B6D4)
- Severity colors: --color-severity-safe (#22C55E), --color-severity-moderate (#F97316), --color-severity-critical (#EF4444)
- Existing animation: animate-fade-in (0.2s ease-out, translateY(4px))

<!-- From copy-button.tsx -->
```typescript
export function CopyButton({ text, label, className }: {
  text: string;
  label: string;
  className?: string;
}) // Renders button with clipboard copy + "Copied!" feedback
```

<!-- From category-card.tsx - reference for mockup styling -->
Category card structure: bg-surface-1 border border-border rounded-lg p-6
Category names: Hidden Logic, Data Access, Action Risk, Permission Scope, Override Attempts
Severity labels with progress bars

<!-- Page structure from Plan 01 -->
page.tsx renders: HeroSection, FeaturesSection, then this plan adds AnimatedMockup and BadgeSnippet below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimatedMockup and BadgeSnippet components</name>
  <files>apps/web/src/components/landing/animated-mockup.tsx, apps/web/src/components/landing/badge-snippet.tsx, apps/web/src/app/globals.css</files>
  <action>
**animated-mockup.tsx** ("use client" -- needs Intersection Observer + useState):

Browser chrome frame:
- Outer container: rounded-xl border border-border-card shadow-card overflow-hidden bg-surface-card, max-w-3xl mx-auto
- Top bar: bg-surface-2 or bg-gray-100 px-4 py-3 flex items-center gap-2 border-b border-border-card
- Three dots (red/yellow/green circles, w-3 h-3 rounded-full) + a fake URL bar (bg-white rounded px-3 py-1 text-xs text-text-muted flex-1 ml-4, showing "skillgate.sh/report/example-skill")

Inside the frame, render a mock audit report:
- Verdict banner: "Safe to Install" with bg-severity-safe/10 text-severity-safe border border-severity-safe/20 rounded-lg p-4 font-semibold
- 5 category rows (hardcoded mock data, NOT imported from audit engine):
  1. Hidden Logic - Safe (green)
  2. Data Access - Safe (green)
  3. Action Risk - Low (yellow/amber)
  4. Permission Scope - Safe (green)
  5. Override Attempts - Safe (green)
- Each row: flex items-center justify-between p-4 border-b border-border-card last:border-b-0, with category name, severity label colored appropriately, and a simple progress bar

Scroll-triggered stagger animation:
- Use a single ref on the container section
- useEffect with IntersectionObserver (threshold: 0.2) to detect when container enters viewport
- When triggered, set `visible` state to true
- Each row gets a CSS class that applies opacity-0 translate-y-4 initially, then transitions to opacity-100 translate-y-0
- Stagger via inline `transitionDelay`: row 0 = 0ms, row 1 = 150ms, row 2 = 300ms, row 3 = 450ms, row 4 = 600ms
- Use CSS transition classes (transition-all duration-500 ease-out) -- no need for keyframe animations
- The verdict banner appears first (delay 0), then rows stagger after it

Add a section heading above the frame like "See it in action" centered, with text-text-heading font-bold text-2xl mb-8.

Wrap in a section with py-16 px-4.

**badge-snippet.tsx** ("use client" -- copy button interaction):

- Section with id="badge-section" (for scroll link from feature card in Plan 01)
- Heading: "Add a trust badge" or similar, centered
- Badge preview area: flex gap-4 justify-center mb-6, showing 3 badge variants:
  - Render 3 inline SVG badges (not img tags to avoid needing real API). Create simple badge SVGs that look like shields:
    - Green "Safe to Install" badge
    - Orange "Use with Caution" badge
    - Red "Avoid / Critical" badge
  - Each is a rounded rect with colored left half and white right half with text (like shields.io style)
  - Use inline SVGs with the severity colors from design tokens
- Below badges: descriptive text like "After auditing your skill, add the badge to your README"
- Markdown snippet block:
  - `![Skillgate](https://skillgate.sh/badge/your-skill)` as the generic placeholder
  - Styled like badge-section.tsx: pre with bg-surface-2 border border-border rounded-lg p-4 font-mono text-sm
  - CopyButton positioned absolute top-right (same pattern as existing badge-section.tsx)
  - Import CopyButton from "@/components/copy-button"

Wrap in a section with py-16 px-4, max-w-2xl mx-auto.

**globals.css** -- no changes needed (existing tokens and animate-fade-in are sufficient). Only add if the stagger animation approach requires new keyframes -- but prefer the CSS transition + transitionDelay approach described above which needs no new CSS.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>Both components render without errors. AnimatedMockup shows browser chrome with mock report data. BadgeSnippet shows 3 badge variants and a copyable markdown snippet.</done>
</task>

<task type="auto">
  <name>Task 2: Wire animated mockup and badge snippet into landing page</name>
  <files>apps/web/src/app/page.tsx</files>
  <action>
Update page.tsx (which already has HeroSection and FeaturesSection from Plan 01):

- Import AnimatedMockup from "@/components/landing/animated-mockup"
- Import BadgeSnippet from "@/components/landing/badge-snippet"
- Add them after FeaturesSection in this order:
  1. HeroSection
  2. FeaturesSection
  3. AnimatedMockup
  4. BadgeSnippet
- page.tsx stays as server component -- client components handle their own "use client" boundary
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>Full landing page renders all 4 sections in order. Build passes. Scroll-triggered animation works in browser. Copy button on badge snippet copies markdown to clipboard.</done>
</task>

</tasks>

<verification>
- `npx turbo build --filter=web` passes with zero errors
- page.tsx imports and renders all 4 landing sections
- AnimatedMockup uses IntersectionObserver for scroll trigger
- BadgeSnippet uses CopyButton from existing component
- Badge section has id="badge-section" for in-page scroll linking
</verification>

<success_criteria>
- Animated mockup visible in browser chrome frame with verdict + 5 category rows
- Rows stagger in with scroll-triggered animation (fade + slide up with delays)
- Badge section shows 3 badge variants (safe/warning/critical) as inline SVGs
- Generic markdown snippet displayed with working copy button
- Full landing page: hero -> features -> mockup -> badge, all rendering cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-page/08-02-SUMMARY.md`
</output>
