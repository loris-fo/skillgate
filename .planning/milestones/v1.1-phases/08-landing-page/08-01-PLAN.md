---
phase: 08-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/page.tsx
  - apps/web/src/components/landing/hero-section.tsx
  - apps/web/src/components/landing/features-section.tsx
autonomous: true
requirements: [LAND-01, LAND-02]

must_haves:
  truths:
    - "Visitor sees bold headline, subtext, and Audit a skill CTA above the fold"
    - "CTA button links to /audit"
    - "Secondary View example report link is visible below primary CTA"
    - "Three feature cards are displayed in a row on desktop, stacked on mobile"
    - "Each feature card is clickable and navigates to its target"
  artifacts:
    - path: "apps/web/src/components/landing/hero-section.tsx"
      provides: "Hero with headline, subtext, primary + secondary CTAs"
      min_lines: 20
    - path: "apps/web/src/components/landing/features-section.tsx"
      provides: "3-column feature card grid"
      min_lines: 40
    - path: "apps/web/src/app/page.tsx"
      provides: "Landing page composing all sections"
  key_links:
    - from: "apps/web/src/app/page.tsx"
      to: "hero-section.tsx, features-section.tsx"
      via: "import and render"
      pattern: "import.*landing"
    - from: "hero-section.tsx"
      to: "/audit"
      via: "next/link href"
      pattern: "href.*audit"
---

<objective>
Build the hero section and feature cards for the landing page.

Purpose: Give visitors an immediate understanding of what Skillgate does and clear paths to take action.
Output: Hero component with CTAs, feature cards grid, and updated page.tsx composing them.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-page/08-CONTEXT.md

<interfaces>
<!-- Design tokens from globals.css -->
Tailwind v4 @theme tokens available:
- bg-bg-page (#F0F9FF), bg-surface-card (#FFF), border-border-card (#BAE6FD)
- text-text-heading (#0C1A1A), text-text-body (#475569), text-text-muted (#94A3B8)
- bg-accent (#06B6D4), bg-accent-hover (#0891B2)
- shadow-card, border-border-card-hover (#06B6D4)
- Backward-compat: bg-surface-0, bg-surface-1, bg-surface-2, bg-surface-3, border-border

Card pattern: rounded-xl, shadow-card, border-border-card, hover:border-border-card-hover

From apps/web/src/app/layout.tsx:
- Layout provides Header + main.flex-1 + Footer
- Pages fill the <main> slot (use section/div, not main)
- Fonts: Plus Jakarta Sans (sans) + Geist Mono (mono)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection and FeaturesSection components</name>
  <files>apps/web/src/components/landing/hero-section.tsx, apps/web/src/components/landing/features-section.tsx</files>
  <action>
Create `apps/web/src/components/landing/` directory with two components:

**hero-section.tsx** (server component, no "use client"):
- Bold, punchy headline -- use tone like "Don't install blind" (per user's direction). Keep it short and attention-grabbing.
- Subtext paragraph: brief explanation of what Skillgate does (trust-verify Claude skills with AI-powered security analysis)
- Primary CTA: `<Link href="/audit">` styled as solid accent button (bg-accent hover:bg-accent-hover text-white rounded-lg px-6 py-3 font-semibold)
- Secondary CTA: `<Link href="/report/... ">` text link "View example report" in text-text-muted with hover:text-accent, pointing to a real report URL. Use `/report/cursor-rules-architect` as a plausible example slug (if it doesn't exist yet, the link still works as a demo -- the point is showing intent).
- Center-aligned, generous vertical padding (py-20 or similar), max-w-3xl mx-auto

**features-section.tsx** (server component, no "use client"):
- Section with heading like "What Skillgate checks" or similar
- 3-column grid on lg (grid-cols-1 lg:grid-cols-3 gap-6), stacks on mobile
- Three cards:
  1. **Security Audit** -- AI analyzes 5 risk categories (hidden logic, data access, action risk, permission scope, override attempts). Icon: Shield or ShieldCheck concept. Links to /audit.
  2. **CLI Gate** -- Install the CLI to block risky skills before they run. Icon: Terminal concept. Links to https://www.npmjs.com/package/skillgate (target="_blank").
  3. **Trust Badges** -- Add verified badges to your README. Icon: Award/Badge concept. Uses `<a href="#badge-section">` to scroll to badge section on page.
- Card styling: rounded-xl bg-surface-card border border-border-card shadow-card hover:border-border-card-hover transition-colors p-6
- Each card: SVG icon (inline, simple 24x24 stroke icons -- draw them manually or use simple path data), heading (font-semibold text-text-heading), description (text-text-body text-sm), entire card is a clickable link
- Use `<Link>` for internal links, `<a>` for external, wrap card content in the link element

Do NOT use any icon library (lucide-react is not installed). Use simple inline SVGs with stroke-based paths for the 3 icons:
- Shield: simple shield outline path
- Terminal: rectangle with "> _" inside
- Award/Badge: circle with ribbon paths
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>Both components render without errors. Hero shows headline + subtext + 2 CTAs. Features shows 3 cards with icons, text, and links in a responsive grid.</done>
</task>

<task type="auto">
  <name>Task 2: Wire landing page and replace current home content</name>
  <files>apps/web/src/app/page.tsx</files>
  <action>
Replace the entire content of `apps/web/src/app/page.tsx`:

- Remove the AuditForm import and current content
- Import HeroSection and FeaturesSection from "@/components/landing/..."
- Render them in order inside a fragment or div:
  1. HeroSection
  2. FeaturesSection
  3. (Plan 02 will add AnimatedMockup and BadgeSnippet below these)
- No wrapping section needed -- each component manages its own spacing and max-width
- Keep as server component (no "use client" needed since child components are also server components)

Note: The AuditForm is being removed from the home page. It will be moved to /audit in Phase 9. This is expected per the CONTEXT.md boundary definition.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page at / shows hero section followed by feature cards. No build errors. AuditForm import removed.</done>
</task>

</tasks>

<verification>
- `npx turbo build --filter=web` passes with zero errors
- page.tsx imports and renders HeroSection and FeaturesSection
- No "use client" directives needed (all server components)
</verification>

<success_criteria>
- Hero visible with bold headline, subtext, primary CTA to /audit, secondary CTA to example report
- Three feature cards visible in grid layout with icons and descriptions
- Cards are clickable with correct link targets
- Responsive: 3-col on desktop, stacked on mobile
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-page/08-01-SUMMARY.md`
</output>
