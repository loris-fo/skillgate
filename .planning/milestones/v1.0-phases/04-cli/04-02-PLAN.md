---
phase: 04-cli
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/cli/src/index.ts
  - packages/cli/src/commands/install.ts
  - packages/cli/tests/commands/install.test.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-04
  - CLI-06

must_haves:
  truths:
    - "skillgate install <url> audits the skill via API and downloads SKILL.md on pass"
    - "skillgate install <url> for High/Critical exits 1 without placing the file"
    - "skillgate install --force <url> places the file regardless of score and exits 0"
    - "skillgate install accepts GitHub raw URLs, skills.sh slugs, HTTP URLs, and local paths"
    - "Install extracts skill name from content for file naming (e.g., commit.md)"
    - "Default placement is .claude/ directory, configurable via --output/-o"
  artifacts:
    - path: "packages/cli/src/index.ts"
      provides: "CLI entry point with Commander setup for install and scan commands"
      contains: "program.command"
    - path: "packages/cli/src/commands/install.ts"
      provides: "Install command handler"
      exports: ["installCommand"]
  key_links:
    - from: "packages/cli/src/commands/install.ts"
      to: "packages/cli/src/lib/api-client.ts"
      via: "auditViaApi call"
      pattern: "import.*auditViaApi.*from.*api-client"
    - from: "packages/cli/src/commands/install.ts"
      to: "packages/cli/src/lib/input-resolver.ts"
      via: "resolveInput call"
      pattern: "import.*resolveInput.*from.*input-resolver"
    - from: "packages/cli/src/commands/install.ts"
      to: "packages/cli/src/lib/gating.ts"
      via: "isBlocked check"
      pattern: "import.*isBlocked.*from.*gating"
    - from: "packages/cli/src/commands/install.ts"
      to: "packages/cli/src/lib/output.ts"
      via: "createOutputHandler for display"
      pattern: "import.*createOutputHandler.*from.*output"
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/src/commands/install.ts"
      via: "Commander action registration"
      pattern: "import.*installCommand.*from.*commands/install"
---

<objective>
Implement the `skillgate install` command and the CLI entry point.

Purpose: Enable developers to run `skillgate install <source>` to audit a skill and download it on pass. This is the primary CLI command covering the audit-and-gate workflow with force override, JSON output, and configurable output directory.

Output: Working install command with Commander entry point, ready for scan command to be added.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli/04-CONTEXT.md
@.planning/phases/04-cli/04-RESEARCH.md
@.planning/phases/04-cli/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 lib modules (will exist after 04-01 completes) -->

From packages/cli/src/types.ts:
```typescript
export type { Score, Verdict, AuditResult, Categories, CategoryResult } from "@skillgate/audit-engine";
export type AuditMeta = { slug: string; url: string; badge_url: string; created_at: string; cached: boolean; };
export type AuditResponse = { result: AuditResult; meta: AuditMeta; };
export type ErrorResponse = { error: { code: string; message: string; }; };
```

From packages/cli/src/lib/input-resolver.ts:
```typescript
export type ResolvedInput = { type: "content"; content: string; name: string } | { type: "url"; url: string };
export function resolveInput(source: string): ResolvedInput;
```

From packages/cli/src/lib/api-client.ts:
```typescript
export function auditViaApi(body: { content: string } | { url: string }): Promise<AuditResponse>;
```

From packages/cli/src/lib/gating.ts:
```typescript
export function isBlocked(score: Score): boolean;
```

From packages/cli/src/lib/output.ts:
```typescript
export function createOutputHandler(jsonMode: boolean): { startSpinner(text: string): { stop(): void; fail(text?: string): void }; printResult(response: AuditResponse): void; printError(msg: string): void; };
export function printCompactResult(response: AuditResponse): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement install command handler</name>
  <files>
    packages/cli/src/commands/install.ts,
    packages/cli/tests/commands/install.test.ts
  </files>
  <behavior>
    - install with passing score: calls resolveInput, calls auditViaApi, checks gating, writes file to output dir, exits 0
    - install with High/Critical score: prints verdict + blocked hint ("Use --force to override."), does NOT write file, exits 1
    - install --force with High/Critical: writes file despite score, exits 0
    - install --json: outputs JSON to stdout, no spinner, no color
    - install with local file: reads file content, sends { content } to API
    - install with URL: sends { url } to API
    - install with registry slug: resolves to skills.sh URL, sends { url } to API
    - File naming: extracts skill name from content (first H1 heading), sanitizes to filesystem-safe name (e.g., "Commit Helper" -> "commit-helper.md")
    - Default output dir is .claude/, configurable via --output/-o
    - Creates output directory if it doesn't exist (mkdir -p equivalent)
    - On API error: spinner.fail(), print error message, exit 1
  </behavior>
  <action>
    1. Create src/commands/install.ts exporting installCommand(source: string, options: { output: string; force: boolean; json: boolean }):
       - Create output handler via createOutputHandler(options.json)
       - Start spinner "Auditing skill..."
       - Call resolveInput(source) to get content or URL
       - Call auditViaApi with resolved input
       - Stop spinner on success, fail on error
       - Print result (JSON or colored)
       - Check gating: if isBlocked(result.overall_score) and NOT options.force, print blocked hint and process.exit(1)
       - Extract skill name for filename: parse first H1 from content. For URL inputs, need to get content from the API response or fetch separately. Since the API returns the full AuditResult but not the raw content, for URL inputs fetch the content separately for file naming and saving. OR use a simpler approach: extract name from result.intent or result.summary. BETTER: for URL inputs, fetch the content first (to also have it for saving), then send { content } to the API. For local files, content is already available.
       - Actually, reconsider: for URL inputs, we need the actual SKILL.md content to save. So: if type is "url", fetch the URL first to get content, then send { content } to audit API. This way we always have content for file naming and saving.
       - Write content to {output}/{sanitized-name}.md using fs.writeFileSync
       - Ensure output directory exists (fs.mkdirSync with recursive: true)
       - Print success message: "Installed to {filepath}"
    2. Write tests that mock api-client and input-resolver modules (vitest vi.mock).
    3. Tests RED first, then implement GREEN.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && pnpm --filter skillgate exec vitest run tests/commands/install.test.ts</automated>
  </verify>
  <done>
    - installCommand handles all input types (URL, local, slug)
    - Gating blocks High/Critical with exit 1, --force overrides
    - JSON mode outputs clean JSON
    - File placed in correct directory with skill-name-based filename
    - All install tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CLI entry point with Commander setup</name>
  <files>
    packages/cli/src/index.ts
  </files>
  <action>
    1. Create src/index.ts as the CLI entry point:
       - Import { Command } from "commander"
       - Import installCommand from "./commands/install.js"
       - Create program with name("skillgate"), description("Audit Claude skills for security risks"), version("0.1.0")
       - Register install command: program.command("install").description("Audit and install a SKILL.md").argument("&lt;source&gt;", "URL, registry slug, or local file path").option("-o, --output &lt;dir&gt;", "target directory", ".claude").option("--force", "override High/Critical block").option("--json", "output machine-readable JSON").action(installCommand)
       - Register scan command stub: program.command("scan").description("Audit all SKILL.md files in the project").option("--path &lt;dir&gt;", "directory to scan").option("--no-fail", "always exit 0 (reporting only)").option("--json", "output machine-readable JSON").action(() => { console.error("scan command not yet implemented"); process.exit(1); })
       - Call program.parse()
    2. Build the CLI: pnpm --filter skillgate build
    3. Test the built CLI: node packages/cli/dist/index.js --help (should show help with install and scan commands)
    4. Test: node packages/cli/dist/index.js --version (should show 0.1.0)
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && pnpm --filter skillgate build && node packages/cli/dist/index.js --help | grep -q "install" && node packages/cli/dist/index.js --version | grep -q "0.1.0" && echo "PASS"</automated>
  </verify>
  <done>
    - src/index.ts exists with Commander program setup
    - Build succeeds producing dist/index.js with shebang
    - --help shows install and scan commands with options
    - --version shows 0.1.0
    - Install command is fully wired and functional
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter skillgate test` passes all tests (including new install tests)
- `pnpm --filter skillgate build` succeeds
- `node packages/cli/dist/index.js --help` shows both commands
- `node packages/cli/dist/index.js install --help` shows install options (--output, --force, --json)
</verification>

<success_criteria>
- `skillgate install` command works end-to-end (with mocked API in tests)
- Gating correctly blocks High/Critical and respects --force
- JSON output is clean and parseable
- CLI builds and runs with shebang
- Commander entry point registered with both install (working) and scan (stub)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-02-SUMMARY.md`
</output>
