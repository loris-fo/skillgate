---
phase: 05-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/package.json
  - packages/cli/README.md
autonomous: true
requirements:
  - INFRA-06

must_haves:
  truths:
    - "npm install -g skillgate installs the CLI and skillgate --version prints 0.1.0"
    - "The published npm tarball contains only dist/index.js, dist/index.d.ts, package.json, and README.md — no .env, no source maps, no workspace:* references"
    - "npx skillgate install <url> works without global install"
    - "prepublishOnly script runs build and tests automatically before publish"
  artifacts:
    - path: "packages/cli/package.json"
      provides: "Complete npm metadata, dependency fix, prepublishOnly script"
      contains: "prepublishOnly"
    - path: "packages/cli/README.md"
      provides: "npm listing page with install instructions and usage examples"
      contains: "npm install -g skillgate"
  key_links:
    - from: "packages/cli/package.json"
      to: "packages/cli/dist/index.js"
      via: "bin field"
      pattern: '"skillgate".*"./dist/index.js"'
    - from: "packages/cli/package.json"
      to: "prepublishOnly"
      via: "lifecycle script"
      pattern: "pnpm run build && pnpm run test"
---

<objective>
Prepare the skillgate CLI package for npm publishing and verify the tarball is clean.

Purpose: INFRA-06 requires the CLI to be npm-publishable. This plan updates package.json with complete metadata, moves @skillgate/audit-engine to devDependencies (tsup inlines types at build time), adds a prepublishOnly safety script, creates a CLI README for the npm listing page, and verifies the tarball contains no sensitive files or workspace references.

Output: A publish-ready packages/cli/ with verified clean tarball.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-publish/05-CONTEXT.md
@.planning/phases/05-publish/05-RESEARCH.md

<interfaces>
<!-- Current packages/cli/package.json state — executor modifies this file -->
From packages/cli/package.json:
```json
{
  "name": "skillgate",
  "version": "0.1.0",
  "type": "module",
  "bin": { "skillgate": "./dist/index.js" },
  "files": ["dist"],
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": { "build": "tsup", "test": "vitest run", "dev": "tsup --watch" },
  "dependencies": {
    "@skillgate/audit-engine": "workspace:*",
    "chalk": "^5.6.0", "cli-table3": "^0.6.5", "commander": "^14.0.0",
    "glob": "^11.0.0", "ora": "^9.3.0", "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.3", "tsup": "^8.5.0", "typescript": "^5.7.0", "vitest": "^3.2.0"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json metadata and fix dependencies</name>
  <files>packages/cli/package.json</files>
  <action>
Update packages/cli/package.json with the following changes:

1. **Move @skillgate/audit-engine from dependencies to devDependencies.** This is critical — audit-engine is NOT published to npm, so leaving it in dependencies would cause consumer install failures. tsup inlines all type imports at build time, so there is zero runtime dependency. Verify by checking dist/index.js has no references to @skillgate/audit-engine.

2. **Add npm metadata fields** (per user locked decisions):
   - `"description": "Audit Claude skills for security risks before installing them"`
   - `"license": "MIT"`
   - `"author": "loris-fo"`
   - `"keywords": ["claude", "skill", "security", "audit", "mcp", "cli"]`
   - `"repository": { "type": "git", "url": "https://github.com/loris-fo/skillgate.git", "directory": "packages/cli" }`
   - `"homepage": "https://github.com/loris-fo/skillgate"` (use GitHub URL since skillgate.dev may not be live)
   - `"bugs": "https://github.com/loris-fo/skillgate/issues"`
   - `"engines": { "node": ">= 18" }`

3. **Add prepublishOnly script** to scripts:
   - `"prepublishOnly": "pnpm run build && pnpm run test"`
   - This runs build first (tests may depend on compiled output), then tests, before every publish/pack

4. **Keep existing fields** unchanged: name, version (0.1.0), type, bin, files, exports, other scripts, remaining dependencies/devDependencies.

Field ordering in package.json: name, version, description, type, license, author, repository, homepage, bugs, keywords, engines, bin, files, exports, scripts, dependencies, devDependencies.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate/packages/cli && node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); const checks = [p.license==='MIT', p.author==='loris-fo', p.engines?.node==='>= 18', p.scripts?.prepublishOnly==='pnpm run build && pnpm run test', !p.dependencies?.['@skillgate/audit-engine'], p.devDependencies?.['@skillgate/audit-engine']==='workspace:*']; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: ' + JSON.stringify(checks))"</automated>
  </verify>
  <done>package.json has MIT license, loris-fo author, engines >= 18, prepublishOnly script, @skillgate/audit-engine in devDependencies only (not in dependencies), and all npm metadata fields present</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI README and verify tarball</name>
  <files>packages/cli/README.md</files>
  <action>
1. **Create packages/cli/README.md** for the npm listing page. This is the first impression for developers discovering skillgate on npm. Structure:

```markdown
# skillgate

Audit Claude skills for security risks before installing them.

## Install

\`\`\`bash
npm install -g skillgate
\`\`\`

Or run without installing:

\`\`\`bash
npx skillgate install <url>
\`\`\`

## Quick Start

\`\`\`bash
# Audit and install a skill from GitHub
skillgate install https://github.com/user/repo/blob/main/.claude/skills/my-skill/SKILL.md

# Scan all skills in current project
skillgate scan
\`\`\`

## Commands

### install

Audit a skill and download it on pass.

\`\`\`
skillgate install <source> [options]
\`\`\`

Sources: GitHub URLs, HTTP URLs, local file paths

| Option | Description |
|--------|-------------|
| `-o, --output <dir>` | Target directory (default: `.claude`) |
| `--force` | Override High/Critical block |
| `--json` | Output machine-readable JSON |

### scan

Audit all skills in the current project.

\`\`\`
skillgate scan [options]
\`\`\`

| Option | Description |
|--------|-------------|
| `--path <dir>` | Directory to scan |
| `--no-fail` | Always exit 0 (reporting only) |
| `--json` | Output machine-readable JSON |

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | All skills passed audit |
| 1 | One or more skills scored High or Critical |

## Requirements

- Node.js >= 18

## Links

- [Website](https://skillgate.dev)
- [GitHub](https://github.com/loris-fo/skillgate)

## License

MIT
```

2. **Run build to ensure dist/ is fresh**, then **verify tarball contents** with `pnpm pack --dry-run` from packages/cli/. Confirm:
   - Only dist/index.js, dist/index.d.ts, package.json, README.md are included
   - No .env, no source maps, no tsup.config.ts, no src/ directory
   - Run `pnpm pack` to create actual tarball, then extract and inspect package.json to confirm no workspace:* references in dependencies

3. **Run prepublishOnly script** to validate it works: `cd packages/cli && pnpm run prepublishOnly` (should build and run tests successfully).

4. **Clean up** the tarball file after verification.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate/packages/cli && pnpm run prepublishOnly && pnpm pack --dry-run 2>&1 | grep -E "README|dist/|package.json" && echo "---" && test -f README.md && echo "README EXISTS"</automated>
  </verify>
  <done>README.md exists with install instructions and usage examples. pnpm pack --dry-run shows only dist/index.js, dist/index.d.ts, package.json, and README.md. prepublishOnly script completes successfully (build + test). No workspace:* references in the packed package.json dependencies.</done>
</task>

</tasks>

<verification>
1. `cd packages/cli && pnpm run prepublishOnly` exits 0 (build + tests pass)
2. `cd packages/cli && pnpm pack --dry-run` shows only: dist/index.js, dist/index.d.ts, package.json, README.md
3. `cd packages/cli && pnpm pack && tar -xf skillgate-0.1.0.tgz package/package.json -O | grep workspace` returns empty (no workspace references in published package.json)
4. `node packages/cli/dist/index.js --version` outputs `0.1.0`
5. `grep -c "audit-engine" packages/cli/dist/index.js` returns 0 (no runtime reference to audit-engine)
</verification>

<success_criteria>
- packages/cli/package.json has complete npm metadata (license, author, repository, keywords, engines, description)
- @skillgate/audit-engine is in devDependencies, not dependencies
- prepublishOnly script runs build + test before publish
- README.md exists with clear install/usage instructions for npm listing
- Tarball verified clean: no sensitive files, no workspace references
- The package is ready for `pnpm publish --access public --no-git-checks` (actual publish is a manual user action)
</success_criteria>

<output>
After completion, create `.planning/phases/05-publish/05-01-SUMMARY.md`
</output>
