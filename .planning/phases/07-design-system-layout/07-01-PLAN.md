---
phase: 07-design-system-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
autonomous: true
requirements: [DS-01, DS-02, DS-03]

must_haves:
  truths:
    - "Background is light sky-blue (#F0F9FF) instead of dark"
    - "Cards are white (#FFFFFF) with #BAE6FD border and subtle shadow"
    - "Accent color is #06B6D4 (cyan) not #8b5cf6 (purple)"
    - "Headings are #0C1A1A, body text is #475569"
    - "Verdict colors green/red/orange are defined as tokens"
    - "Font is Plus Jakarta Sans for body, Geist Mono for code"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "All design tokens via @theme block"
      contains: "--color-bg-page: #F0F9FF"
    - path: "apps/web/src/app/layout.tsx"
      provides: "Font loading and html element setup"
      contains: "Plus_Jakarta_Sans"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/app/globals.css"
      via: "CSS variable font references"
      pattern: "--font-sans.*--font-plus-jakarta"
---

<objective>
Replace the dark terminal theme with the new light sky-blue design system tokens and swap fonts to Plus Jakarta Sans / Geist Mono.

Purpose: Establish the visual foundation (colors, typography, spacing tokens) that all subsequent layout and page work builds on.
Output: Updated globals.css with light theme tokens, updated layout.tsx with new fonts and no dark class.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-system-layout/07-CONTEXT.md
@apps/web/src/app/globals.css
@apps/web/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace design tokens in globals.css</name>
  <files>apps/web/src/app/globals.css</files>
  <action>
Replace the entire @theme block in globals.css with the new light design system tokens.

Remove the `@custom-variant dark` line and the dark theme colors entirely.

New @theme values:
- Font variables: `--font-sans: var(--font-plus-jakarta-sans);` and `--font-mono: var(--font-geist-mono);`
- Background: `--color-bg-page: #F0F9FF;`
- Card surface: `--color-surface-card: #FFFFFF;`
- Card border: `--color-border-card: #BAE6FD;`
- Card border hover: `--color-border-card-hover: #06B6D4;`
- Card shadow: use `--shadow-card: 0 1px 3px 0 rgb(0 0 0 / 0.05);` (shadow-sm equivalent)
- Accent: `--color-accent: #06B6D4;` and `--color-accent-hover: #0891B2;` (darker cyan)
- Headings: `--color-text-heading: #0C1A1A;`
- Body text: `--color-text-body: #475569;`
- Muted text: `--color-text-muted: #94A3B8;`
- Verdict/severity colors: keep `--color-severity-safe: #22C55E;`, `--color-severity-critical: #EF4444;`, `--color-severity-high: #F97316;`, and keep existing low/moderate values
- Keep the fade-in animation keyframes

Also add convenience token aliases for backward compat with existing components that reference old names. Map old semantic names to new values so existing pages don't break:
- `--color-surface-0: #F0F9FF;` (was dark bg, now page bg)
- `--color-surface-1: #FFFFFF;` (was dark card, now white card)
- `--color-surface-2: #F8FAFC;` (slightly off-white for subtle sections)
- `--color-surface-3: #E0F2FE;` (was dark hover bg, now light blue)
- `--color-border: #BAE6FD;` (was dark border, now card border)
- `--color-text-primary: #0C1A1A;` (was light text, now heading dark)
- `--color-text-secondary: #475569;` (was gray text, now body text)
- `--color-accent-muted: #06B6D420;` (accent with alpha)
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -c "F0F9FF" apps/web/src/app/globals.css && grep -c "06B6D4" apps/web/src/app/globals.css && grep -c "dark" apps/web/src/app/globals.css | grep "^0$"</automated>
  </verify>
  <done>globals.css contains all new light theme tokens, no dark theme references, backward-compat aliases for existing component class names</done>
</task>

<task type="auto">
  <name>Task 2: Swap fonts and remove dark class in layout.tsx</name>
  <files>apps/web/src/app/layout.tsx</files>
  <action>
Replace font imports in layout.tsx:
- Remove `Inter` and `JetBrains_Mono` imports from `next/font/google`
- Add `Plus_Jakarta_Sans` from `next/font/google` with `subsets: ["latin"], variable: "--font-plus-jakarta-sans", display: "swap", weight: ["400", "500", "600", "700", "800"]`
- Add `Geist_Mono` from `next/font/google` with `subsets: ["latin"], variable: "--font-geist-mono", display: "swap"`

Update the html element:
- Remove `dark` from className
- Replace old font variables with new: `${plusJakartaSans.variable} ${geistMono.variable}`

Update body className:
- Replace `bg-surface-0` with `bg-bg-page` (maps to #F0F9FF)
- Replace `text-text-primary` with `text-text-heading` (or keep text-text-primary since we aliased it)
- Keep `font-sans antialiased min-h-screen`
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep "Plus_Jakarta_Sans" apps/web/src/app/layout.tsx && grep -v "dark" apps/web/src/app/layout.tsx | grep -q "html" && echo "OK"</automated>
  </verify>
  <done>layout.tsx loads Plus Jakarta Sans and Geist Mono, html element has no dark class, body uses new bg token</done>
</task>

</tasks>

<verification>
Run `cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web` — build must succeed with no errors. Existing pages should render (they may look different but should not crash).
</verification>

<success_criteria>
- globals.css has all light theme design tokens defined
- No dark class on html element
- Plus Jakarta Sans and Geist Mono loaded as font variables
- Build passes without errors
- Existing component class names (surface-0, surface-1, border, text-primary, etc.) still resolve to valid colors via aliases
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-layout/07-01-SUMMARY.md`
</output>
