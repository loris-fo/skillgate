---
phase: 03-web-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/category-card.tsx
  - apps/web/src/components/__tests__/category-card.test.tsx
autonomous: true
gap_closure: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-04
  - WEB-05
  - WEB-07
  - WEB-08
  - WEB-09

must_haves:
  truths:
    - "Category cards show header with severity label and progress bar in collapsed state"
    - "Clicking a category card header expands it to reveal finding and detail text"
    - "Clicking again collapses the detail back"
    - "A visual chevron indicator signals expand/collapse state"
    - "Categories start expanded by default so users see full content on first load"
  artifacts:
    - path: "apps/web/src/components/category-card.tsx"
      provides: "Interactive expand/collapse category card"
      contains: "use client"
    - path: "apps/web/src/components/__tests__/category-card.test.tsx"
      provides: "Component tests for expand/collapse behavior"
      contains: "click"
  key_links:
    - from: "apps/web/src/components/category-card.tsx"
      to: "apps/web/src/app/report/[slug]/page.tsx"
      via: "CategoryCard import — no changes needed to consumer"
      pattern: "CategoryCard"
---

<objective>
Add expand/collapse interaction to CategoryCard to close the WEB-03 verification gap.

Purpose: The VERIFICATION.md found that category cards render fully expanded always with no click-to-reveal interaction, contradicting WEB-03 ("expandable rows — clicking a category reveals the plain-English reasoning") and ROADMAP SC#3.

Output: Interactive CategoryCard component with toggle behavior and a component test verifying the interaction.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-ui/03-VERIFICATION.md

<interfaces>
<!-- Current CategoryCard interface — props unchanged, only internal behavior changes -->

From apps/web/src/components/category-card.tsx:
```typescript
import type { CategoryResult } from "@skillgate/audit-engine";
import { SEVERITY_CONFIG } from "@/lib/severity";

export function CategoryCard({ name, result }: { name: string; result: CategoryResult });
```

From apps/web/src/lib/severity.ts:
```typescript
export const SEVERITY_CONFIG: Record<Score, { color: string; bg: string; percent: number; label: string }>;
```

From apps/web/src/app/report/[slug]/page.tsx (consumer — no changes needed):
```typescript
import { CategoryCard } from "@/components/category-card";
// Used as: <CategoryCard key={name} name={name} result={cat} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add expand/collapse interaction to CategoryCard</name>
  <files>apps/web/src/components/category-card.tsx, apps/web/src/components/__tests__/category-card.test.tsx</files>
  <behavior>
    - Test 1: CategoryCard renders with category label and severity badge visible
    - Test 2: CategoryCard starts with finding and detail text visible (expanded by default)
    - Test 3: Clicking the header collapses the card — finding and detail text are hidden
    - Test 4: Clicking again expands the card — finding and detail text reappear
    - Test 5: A chevron icon rotates to indicate expanded/collapsed state
    - Test 6: by_design badge renders when result.by_design is true
  </behavior>
  <action>
Convert CategoryCard to a Client Component ("use client") with useState for expanded state (default: true — categories start expanded so users see full content on first load, but CAN collapse them).

Implementation details:
- Add "use client" directive at top of file
- Add `const [expanded, setExpanded] = useState(true)` state
- Make the header row (`h3` + severity label area) a clickable `<button>` with `onClick={() => setExpanded(!expanded)}` and `cursor-pointer` styling
- Add an inline SVG chevron (simple down-arrow, 16x16) that rotates 180deg when collapsed. Use `transition-transform duration-200` for subtle animation per user decision ("minimal motion").
- Wrap finding + detail text in a `<div>` that conditionally renders based on `expanded` state. Use CSS `overflow-hidden transition-all duration-200` with `max-height` or simple conditional rendering (`{expanded && (...)}`) — conditional rendering is simpler and avoids layout measurement complexity.
- Progress bar remains always visible (part of the header summary)
- Keep the same props interface — no changes needed to consumers
- Preserve all existing Tailwind classes, theme tokens, and visual styling
- The button should have `aria-expanded={expanded}` for accessibility
- Add `w-full text-left` to the button so it behaves like a block-level click target

Do NOT:
- Add an "expand all / collapse all" feature (not in requirements)
- Change the props interface (would break the report page consumer)
- Use a third-party accordion library (overkill for one toggle)
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && pnpm --filter @skillgate/web test -- src/components/__tests__/category-card.test.tsx</automated>
  </verify>
  <done>CategoryCard is interactive: clicking the header toggles visibility of finding/detail text. Chevron rotates to indicate state. Cards start expanded by default. All 6 behavior tests pass. The report page renders identically on first load (expanded) but users can now collapse categories.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @skillgate/web test` — all tests pass including new category-card tests
2. `pnpm --filter @skillgate/web build` — build succeeds (Client Component properly declared)
3. CategoryCard export interface unchanged — report page needs zero modifications
</verification>

<success_criteria>
- CategoryCard has expand/collapse toggle interaction
- Clicking header hides/shows finding and detail text
- Visual chevron indicator shows expanded/collapsed state
- Cards default to expanded (preserving current visual on first load)
- Component test covers toggle behavior
- WEB-03 gap from VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-04-SUMMARY.md`
</output>
