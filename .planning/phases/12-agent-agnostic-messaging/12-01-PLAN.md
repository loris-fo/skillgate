---
phase: 12-agent-agnostic-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/landing/hero-section.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/audit/page.tsx
  - apps/web/src/components/audit-form.tsx
  - apps/web/src/app/report/[slug]/opengraph-image.tsx
  - packages/cli/src/index.ts
  - packages/cli/package/package.json
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
  - COPY-04

must_haves:
  truths:
    - "Landing page hero subtitle says 'AI agent skill' with zero Claude-specific language anywhere on the page"
    - "Audit page form label reads 'Skill file content' (not 'SKILL.md Content') and placeholder references multiple agents"
    - "Running skillgate --help shows 'AI agent skills' language with no Claude-specific references"
    - "The npm package description reads as a universal security auditor for AI agent skills"
  artifacts:
    - path: "apps/web/src/components/landing/hero-section.tsx"
      provides: "Agent-agnostic hero subtitle"
      contains: "AI agent skill"
    - path: "apps/web/src/components/audit-form.tsx"
      provides: "Agent-agnostic form labels"
      contains: "Skill file content"
    - path: "packages/cli/src/index.ts"
      provides: "Agent-agnostic CLI descriptions"
      contains: "AI agent skill"
    - path: "packages/cli/package/package.json"
      provides: "Agent-agnostic npm description"
      contains: "AI agent skill"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "meta description"
      via: "Metadata export"
      pattern: "AI agent skill"
    - from: "apps/web/src/app/audit/page.tsx"
      to: "meta description"
      via: "Metadata export"
      pattern: "AI agent skill"
---

<objective>
Update all user-facing copy across web app and CLI from Claude-specific language to universal agent-agnostic language.

Purpose: SkillGate supports auditing skills for any AI agent (Claude, Cursor, Windsurf, Cline, Copilot, Aider), so all messaging must reflect this universal scope.
Output: Every user-facing string in the web app and CLI uses "AI agent skills" instead of "Claude skills" or "SKILL.md".
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update web app copy to agent-agnostic language</name>
  <files>
    apps/web/src/components/landing/hero-section.tsx
    apps/web/src/app/layout.tsx
    apps/web/src/app/audit/page.tsx
    apps/web/src/components/audit-form.tsx
    apps/web/src/app/report/[slug]/opengraph-image.tsx
  </files>
  <action>
Update all Claude-specific and SKILL.md-specific copy in the web app. Exact changes:

**hero-section.tsx** (line 45):
- Change "Trust-verify any Claude skill before it touches your codebase." to "Trust-verify any AI agent skill before it touches your codebase."

**layout.tsx** (line 24):
- Change `description: "Trust-verify Claude skills before installing them"` to `description: "Trust-verify AI agent skills before installing them"`

**audit/page.tsx**:
- Line 7: Change meta description from "Paste a SKILL.md or provide a URL to audit any Claude skill for security risks." to "Paste skill file content or provide a URL to audit any AI agent skill for security risks."
- Line 30: Keep heading "Audit your AI skills with confidence." (already agent-agnostic)
- Line 42-43: Change "Paste your SKILL.md content or provide a URL to get a full security audit with plain-English reasoning." to "Paste your skill file content or provide a URL to get a full security audit with plain-English reasoning."

**audit-form.tsx**:
- Line 100: Change placeholder from "https://github.com/user/repo/raw/.../SKILL.md" to "https://github.com/user/repo/raw/.../skill-file.md"
- Line 120: Change label from "SKILL.md Content" to "Skill file content"
- Line 132: Change placeholder from "Paste SKILL.md content here..." to "Paste skill file content here..."

**opengraph-image.tsx** (line 26):
- Change `let summary = "Security audit for Claude skills"` to `let summary = "Security audit for AI agent skills"`

Do NOT change any component logic, styling, or structure. Only change string literals.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -r "Claude" apps/web/src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".next" | grep -v "// " | grep -v test</automated>
  </verify>
  <done>Zero Claude-specific references in web app source files (excluding tests, comments). Hero subtitle says "AI agent skill". Audit form label says "Skill file content". Meta descriptions updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI help text and npm package metadata</name>
  <files>
    packages/cli/src/index.ts
    packages/cli/package/package.json
  </files>
  <action>
Update all Claude-specific copy in the CLI package. Exact changes:

**packages/cli/src/index.ts**:
- Line 9: Change `.description("Audit Claude skills for security risks")` to `.description("Audit AI agent skills for security risks")`
- Line 14: Change `.description("Audit and install a SKILL.md")` to `.description("Audit and install an AI agent skill file")`
- Line 23: Change `.description("Audit all SKILL.md files in the project")` to `.description("Audit all skill files in the project")`

**packages/cli/package/package.json**:
- Change `"description": "Audit Claude skills for security risks before installing them"` to `"description": "Security auditor for AI agent skills — audit before you install"`
- Update keywords array: replace `"claude"` with `"ai-agent"`, add `"cursor"`, `"windsurf"`, `"copilot"`. Keep existing `"skill"`, `"security"`, `"audit"`, `"mcp"`, `"cli"` keywords.

Do NOT change any command logic, options, or argument definitions.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -r "Claude" packages/cli/src/ --include="*.ts" | grep -v node_modules | grep -v test | grep -v "// "</automated>
  </verify>
  <done>Zero Claude-specific references in CLI source files (excluding tests, comments). Running `node packages/cli/dist/index.js --help` would show "AI agent skills" language. Package description updated.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Grep for remaining "Claude" references in production source files:
   ```
   grep -r "Claude" apps/web/src/ packages/cli/src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v "// " | grep -v node_modules
   ```
   Expected: No results (zero Claude-specific references in production code)

2. Grep for remaining "SKILL.md" in user-facing strings:
   ```
   grep -r "SKILL\.md" apps/web/src/ packages/cli/src/index.ts --include="*.ts" --include="*.tsx" | grep -v test | grep -v "// "
   ```
   Expected: No results in user-facing labels/placeholders (internal code references like slug.ts are fine)

3. Run existing tests to verify no regressions:
   ```
   cd apps/web && pnpm test
   cd packages/cli && pnpm test
   ```
</verification>

<success_criteria>
- Landing page hero subtitle mentions "AI agent skill" with no Claude-specific language
- Audit page form label reads "Skill file content" and placeholder references generic skill files
- CLI help text uses "AI agent skills" language throughout
- npm package description reads as a universal security auditor for AI agent skills
- All existing tests pass (or only fail for unrelated reasons)
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-agnostic-messaging/12-01-SUMMARY.md`
</output>
