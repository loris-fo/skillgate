---
phase: 12-agent-agnostic-messaging
plan: 02
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - apps/web/src/components/audit-form.tsx
autonomous: true
requirements:
  - COPY-02
gap_closure: true

must_haves:
  truths:
    - "Audit page textarea placeholder explicitly references multiple agents by name (Claude, Cursor, Windsurf)"
  artifacts:
    - path: "apps/web/src/components/audit-form.tsx"
      provides: "Multi-agent placeholder text in textarea"
      contains: "Claude, Cursor, Windsurf"
  key_links: []
---

<objective>
Close COPY-02 verification gap: update the audit form textarea placeholder to explicitly reference multiple agents.

Purpose: The ROADMAP success criterion #2 requires "placeholder text references multiple agents." The current placeholder reads "Paste skill file content here..." which is generic but does not name specific agents.
Output: Updated placeholder in audit-form.tsx that names Claude, Cursor, Windsurf.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-agnostic-messaging/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-agent reference to audit form textarea placeholder</name>
  <files>apps/web/src/components/audit-form.tsx</files>
  <action>
In `apps/web/src/components/audit-form.tsx`, update line 132:

Change:
```
placeholder="Paste skill file content here..."
```

To:
```
placeholder="Paste skill file content here (Claude, Cursor, Windsurf, and more)..."
```

This is a single string literal change. Do NOT modify any component logic, styling, imports, or other lines. Only change the placeholder text on this one textarea element.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -n "Claude, Cursor, Windsurf" apps/web/src/components/audit-form.tsx</automated>
  </verify>
  <done>Textarea placeholder on the audit page explicitly names Claude, Cursor, and Windsurf as supported agents, satisfying ROADMAP success criterion #2.</done>
</task>

</tasks>

<verification>
1. Confirm multi-agent reference in placeholder:
   ```
   grep "Claude, Cursor, Windsurf" apps/web/src/components/audit-form.tsx
   ```
   Expected: One match on the textarea placeholder line.

2. Confirm no other unintended changes:
   ```
   git diff --stat apps/web/src/components/audit-form.tsx
   ```
   Expected: 1 file changed, 1 insertion, 1 deletion.
</verification>

<success_criteria>
- The audit form textarea placeholder reads "Paste skill file content here (Claude, Cursor, Windsurf, and more)..."
- No other files or lines are modified
- COPY-02 verification gap is fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-agnostic-messaging/12-02-SUMMARY.md`
</output>
