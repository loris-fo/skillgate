---
phase: 09-audit-report-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/audit/page.tsx
  - apps/web/src/components/audit-form.tsx
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "Visiting /audit shows a centered card with heading, subtext, URL input, and textarea on sky-blue background"
    - "Submitting a valid URL or pasted SKILL.md triggers audit and redirects to /report/[slug]"
    - "Full-page semi-transparent overlay with spinner appears during audit loading"
    - "Landing page CTA 'Audit a skill' navigates to /audit successfully"
  artifacts:
    - path: "apps/web/src/app/audit/page.tsx"
      provides: "Audit page route wrapping AuditForm in card layout"
      min_lines: 15
    - path: "apps/web/src/components/audit-form.tsx"
      provides: "Updated form with full-page overlay loading state"
      contains: "fixed inset-0"
  key_links:
    - from: "apps/web/src/app/audit/page.tsx"
      to: "apps/web/src/components/audit-form.tsx"
      via: "import and render AuditForm"
      pattern: "import.*AuditForm"
    - from: "apps/web/src/components/audit-form.tsx"
      to: "/api/audit"
      via: "fetch POST on submit"
      pattern: "fetch.*api/audit"
---

<objective>
Create the /audit route page and update AuditForm to match the new design system.

Purpose: Users need a dedicated /audit page (currently no route exists) with the new light design, and the loading state should use a full-page overlay instead of inline replacement.
Output: Working /audit page with card-based layout and polished loading overlay.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audit-report-pages/09-CONTEXT.md

<interfaces>
<!-- Existing AuditForm component (client component with form state, validation, fetch to /api/audit) -->
<!-- Will be modified in-place — no new exports needed -->

From apps/web/src/lib/types.ts:
```typescript
export type AuditResponse = { result: AuditResult; meta: AuditMeta };
export type ErrorResponse = { error: { code: ApiErrorCode; message: string } };
```

Design system card pattern (from Phase 7):
```
rounded-xl bg-surface-card border border-border-card shadow-card p-6
```

Backward-compat aliases also work:
```
bg-surface-1 border border-border rounded-lg
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /audit route page</name>
  <files>apps/web/src/app/audit/page.tsx</files>
  <action>
Create a new Next.js page at apps/web/src/app/audit/page.tsx (server component). Layout:

1. Export metadata: `title: "Audit a Skill — Skillgate"`, `description: "Paste a SKILL.md or provide a URL to audit any Claude skill for security risks."`
2. Outer wrapper: `section` with `max-w-2xl mx-auto py-12 px-4 animate-fade-in`
3. Inside, a card div: `rounded-xl bg-surface-card border border-border-card shadow-card p-8`
4. Inside card:
   - `h1` with `text-2xl font-bold text-text-heading mb-2`: "Audit a Skill"
   - `p` with `text-text-body mb-6`: "Paste your SKILL.md content or provide a URL to get a full security audit with plain-English reasoning."
   - Import and render `<AuditForm />` from `@/components/audit-form`

This page will be picked up by the existing layout.tsx (Header + Footer already wrap all pages).
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>Visiting /audit shows a centered white card on sky-blue background with heading, subtext, and the audit form fields (URL input + textarea).</done>
</task>

<task type="auto">
  <name>Task 2: Update AuditForm loading state to full-page overlay</name>
  <files>apps/web/src/components/audit-form.tsx</files>
  <action>
Modify the existing AuditForm component's loading state. Currently when `loading` is true, the entire form is replaced with a centered spinner div. Change to:

1. Keep the form visible at all times (remove the early `if (loading) return ...` block)
2. When `loading` is true, render a full-page overlay ON TOP of the form using a fixed-position div:
   - Outer div: `fixed inset-0 z-50 flex items-center justify-center bg-black/30 backdrop-blur-sm`
   - Inner content: white rounded card (`rounded-xl bg-white p-8 shadow-lg flex flex-col items-center gap-4`)
   - Spinner: `div` with `h-10 w-10 rounded-full border-2 border-accent border-t-transparent animate-spin`
   - Text: `p` with `text-text-heading text-lg font-medium`: "Auditing skill..."
   - Subtext: `p` with `text-text-body text-sm`: "This usually takes a few seconds"
3. Also disable form inputs while loading by adding `disabled={loading}` to both the URL input and textarea, and reduce their opacity with `opacity-60` when loading is true.
4. Keep the submit button's existing `disabled={!canSubmit}` logic — `canSubmit` already checks `!loading`.

Do NOT change any form submission logic, validation, error handling, or redirect behavior. Visual-only change to loading state presentation.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>When audit is in progress, a semi-transparent overlay with centered spinner appears over the full page while the form remains visible underneath. Form inputs are disabled during loading. All existing submission/redirect logic unchanged.</done>
</task>

</tasks>

<verification>
- `npx turbo build --filter=web` succeeds with no errors
- /audit route exists and renders AuditForm inside a card
- Loading state shows full-page overlay (not inline replacement)
- Form submission still POSTs to /api/audit and redirects to /report/[slug]
</verification>

<success_criteria>
1. /audit page renders with card layout matching design system (sky-blue bg, white card, proper typography)
2. Both URL and textarea fields visible simultaneously (no tabs)
3. Full-page semi-transparent overlay with spinner appears during loading
4. Existing form submission, validation, error handling, and redirect all work unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-audit-report-pages/09-01-SUMMARY.md`
</output>
