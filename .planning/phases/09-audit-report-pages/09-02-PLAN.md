---
phase: 09-audit-report-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/severity.ts
  - apps/web/src/components/report-hero.tsx
  - apps/web/src/components/category-card.tsx
  - apps/web/src/components/utility-section.tsx
  - apps/web/src/components/badge-section.tsx
  - apps/web/src/app/report/[slug]/page.tsx
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03]

must_haves:
  truths:
    - "Report page shows verdict as a large colored pill badge next to skill summary"
    - "Numeric score displayed as 'X/10' format with severity color, no progress bar in verdict"
    - "Copy link button is prominent next to the verdict badge area"
    - "Recommendation details (Best for, Caveats, Alternatives) in a separate card below verdict"
    - "Category cards show colored pill labels instead of progress bars, in a 2-column responsive grid"
    - "Section headings visible above each section (Security Analysis, Recommendation, Utility, Badge)"
    - "'Audit another skill' link at the very bottom navigates to /audit"
    - "Badge image, copy snippet, and shareable URL all remain functional"
  artifacts:
    - path: "apps/web/src/lib/severity.ts"
      provides: "SEVERITY_CONFIG with numeric field for X/10 score display"
      contains: "numeric"
    - path: "apps/web/src/components/report-hero.tsx"
      provides: "Verdict badge pill, numeric score display, copy link, summary"
      contains: "rounded-full"
    - path: "apps/web/src/components/category-card.tsx"
      provides: "Category cards with pill labels, expand/collapse, no progress bar"
      contains: "rounded-full"
    - path: "apps/web/src/components/utility-section.tsx"
      provides: "Utility analysis in design system card"
      contains: "surface-card"
    - path: "apps/web/src/components/badge-section.tsx"
      provides: "Badge preview and snippet in design system card"
      contains: "surface-card"
    - path: "apps/web/src/app/report/[slug]/page.tsx"
      provides: "Report page with section headings, recommendation card, grid categories, audit-another link"
      contains: "Audit another skill"
  key_links:
    - from: "apps/web/src/app/report/[slug]/page.tsx"
      to: "apps/web/src/components/report-hero.tsx"
      via: "import ReportHero"
      pattern: "import.*ReportHero"
    - from: "apps/web/src/app/report/[slug]/page.tsx"
      to: "/audit"
      via: "Link at bottom of page"
      pattern: "href.*audit"
    - from: "apps/web/src/components/category-card.tsx"
      to: "apps/web/src/lib/severity.ts"
      via: "SEVERITY_CONFIG for pill colors"
      pattern: "SEVERITY_CONFIG"
    - from: "apps/web/src/components/report-hero.tsx"
      to: "apps/web/src/lib/severity.ts"
      via: "SEVERITY_CONFIG.numeric for X/10 score"
      pattern: "severity\\.numeric"
---

<objective>
Redesign the report page with verdict badge pills, card-based layout, 2-column category grid, and "Audit another skill" link.

Purpose: The report page needs to match the new design system with a cleaner, more scannable layout. Verdict as a colored pill (GitHub-label style), category pills replacing progress bars, and proper section flow.
Output: Fully redesigned /report/[slug] page with all existing data and functionality intact.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audit-report-pages/09-CONTEXT.md

<interfaces>
From apps/web/src/lib/severity.ts:
```typescript
export const SEVERITY_CONFIG: Record<Score, { color: string; bg: string; percent: number; label: string }>;
export const VERDICT_CONFIG: Record<string, { color: string; bg: string; label: string }>;
```

From apps/web/src/lib/types.ts:
```typescript
export type AuditMeta = { slug: string; url: string; badge_url: string; created_at: string; cached: boolean };
export type AuditResponse = { result: AuditResult; meta: AuditMeta };
```

From @skillgate/audit-engine (used by components):
```typescript
type Score = "safe" | "low" | "moderate" | "high" | "critical";
type CategoryResult = { score: Score; finding: string; detail: string; by_design: boolean };
type UtilityAnalysis = { what_it_does: string; use_cases: string[]; not_for: string[]; trigger_behavior: string; dependencies: string[] };
type AuditResult = {
  summary: string;
  overall_score: Score;
  categories: Record<string, CategoryResult>;
  recommendation: { verdict: string; for_who: string; caveats: string[]; alternatives: string[] };
  utility_analysis: UtilityAnalysis;
};
```

Design system card pattern:
```
rounded-xl bg-surface-card border border-border-card shadow-card p-6
```

CopyButton component (reuse as-is):
```typescript
export function CopyButton({ text, label, className }: { text: string; label: string; className?: string });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add numeric score to SEVERITY_CONFIG and redesign report-hero with verdict pill</name>
  <files>apps/web/src/lib/severity.ts, apps/web/src/components/report-hero.tsx</files>
  <action>
**severity.ts** — Add a `numeric` field to SEVERITY_CONFIG for the X/10 score display (per user decision: "Numeric score only (e.g. 3/10) with severity color"):

Update the type to include `numeric: number` and add values:
- safe: numeric 2
- low: numeric 4
- moderate: numeric 6
- high: numeric 8
- critical: numeric 10

These represent risk severity on a 1-10 scale (higher = more risky). Keep all existing fields (color, bg, percent, label) unchanged.

**report-hero.tsx** — Rewrite to match the new design. Keep it as a server component. Keep all existing imports and data logic (VERDICT_CONFIG, SEVERITY_CONFIG, permalink generation, CopyButton).

New layout inside a design system card (`rounded-xl bg-surface-card border border-border-card shadow-card p-8 mb-8`):

1. **Top row** (flex, items-center, gap-4, flex-wrap):
   - Verdict pill: `span` with `inline-flex items-center px-4 py-1.5 rounded-full text-sm font-semibold text-white` and dynamic bg class from `verdict.bg` (e.g., `bg-severity-safe`). Text = `verdict.label` (e.g., "Install", "Avoid").
   - Score pill: `span` with `inline-flex items-center px-3 py-1 rounded-full text-sm font-medium border` using `severity.color` for text and a matching border. Content = `${severity.numeric}/10` (e.g., "2/10", "8/10"). This renders the numeric risk score per user decision.
   - CopyButton: render `<CopyButton text={permalink} label="Copy Link" />` inline in this row.

2. **Summary** (below top row, mt-4):
   - `p` with `text-text-body text-base leading-relaxed`: `result.summary`

REMOVE the recommendation section (for_who, caveats, alternatives) from this component entirely. It will be rendered separately in the report page.

REMOVE the progress bar. REMOVE the colored top bar. REMOVE the "Overall Score" label with the old layout.

Export a new standalone component `RecommendationCard` from the SAME file for the recommendation details:

```typescript
export function RecommendationCard({ recommendation }: { recommendation: AuditResult["recommendation"] }) {
```

Layout for RecommendationCard — a design system card (`rounded-xl bg-surface-card border border-border-card shadow-card p-6`):
- Only render if recommendation has for_who, caveats, or alternatives
- "Best for" paragraph if `for_who` exists: label in `text-text-muted text-xs uppercase tracking-wide font-medium`, value in `text-text-body text-sm`
- "Caveats" list if `caveats.length > 0`: same label style, `ul` with `list-disc list-inside text-sm text-severity-moderate`
- "Alternatives" list if `alternatives.length > 0`: same label style, `ul` with `list-disc list-inside text-sm text-text-body`
- Space each subsection with `space-y-4`
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>SEVERITY_CONFIG has numeric field (safe=2, low=4, moderate=6, high=8, critical=10). ReportHero shows verdict as colored pill badge + numeric score pill in "X/10" format with severity color + copy link in a row, with summary below. RecommendationCard exported separately. No progress bars.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign category-card with pill labels and update report page layout</name>
  <files>apps/web/src/components/category-card.tsx, apps/web/src/components/utility-section.tsx, apps/web/src/components/badge-section.tsx, apps/web/src/app/report/[slug]/page.tsx</files>
  <action>
**category-card.tsx** — Keep as client component with expand/collapse. Changes:

1. Use design system card: `rounded-xl bg-surface-card border border-border-card shadow-card p-6`
2. Replace progress bar with a colored pill label next to category name in the header button:
   - Pill: `span` with `inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-semibold text-white` and `severity.bg` class. Text = `severity.label`.
3. Keep "By Design" badge as-is (accent-muted pill)
4. Keep expand/collapse chevron SVG and aria-expanded
5. Remove the entire progress bar div (lines with `h-2 rounded-full bg-surface-3`)
6. Keep finding + detail text rendering in expanded state
7. Default `expanded` to `false` (was `true`) — collapsed by default is better for a grid view

**utility-section.tsx** — Keep as server component. Changes:

1. Use design system card: `rounded-xl bg-surface-card border border-border-card shadow-card p-6 mb-0` (remove my-8, spacing handled by parent)
2. Keep all content structure (what_it_does, use_cases, not_for, trigger_behavior, dependencies)
3. Update heading: `text-text-heading` instead of `text-text-primary`

**badge-section.tsx** — Keep as server component. Changes:

1. Use design system card: `rounded-xl bg-surface-card border border-border-card shadow-card p-6 mb-0` (remove my-8)
2. Update heading: `text-text-heading`
3. Badge preview bg: `bg-surface-0 rounded-lg p-6` (lighter bg for contrast)
4. Snippet pre: keep existing styling, it works well

**report/[slug]/page.tsx** — Update page layout. Changes:

1. Import `RecommendationCard` from `@/components/report-hero` (new export from Task 1)
2. Import `Link` from `next/link`
3. Section order with visible headings:
   - `<ReportHero result={report.result} meta={report.meta} />`
   - Section heading `h2` "Security Analysis" with `text-text-heading text-xl font-semibold mb-4`
   - Category cards in a 2-column responsive grid: `div` with `grid grid-cols-1 md:grid-cols-2 gap-4`
   - Section heading `h2` "Recommendation" with `text-text-heading text-xl font-semibold mt-8 mb-4`
   - `<RecommendationCard recommendation={report.result.recommendation} />`
   - Section heading `h2` "Utility" with `text-text-heading text-xl font-semibold mt-8 mb-4`
   - `<UtilitySection utility={report.result.utility_analysis} />`
   - Section heading `h2` "Badge" with `text-text-heading text-xl font-semibold mt-8 mb-4`
   - `<BadgeSection slug={report.meta.slug} />`
   - "Audit another skill" link at the bottom: `Link` with `href="/audit"` styled as `inline-flex items-center text-accent hover:text-accent-hover font-medium mt-8 transition-colors`. Text: "Audit another skill" with a left arrow or simple text link.
4. Keep the outer wrapper: `section` with `max-w-4xl mx-auto py-8 px-4 animate-fade-in`
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && npx turbo build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <done>Report page shows: verdict pill + numeric X/10 score + category grid (2-col, pills, no progress bars) + recommendation card + utility + badge + "Audit another skill" link. All data renders correctly, badge/copy/shareable URL functional.</done>
</task>

</tasks>

<verification>
- `npx turbo build --filter=web` succeeds with no errors
- Report page section order: Verdict -> Categories -> Recommendation -> Utility -> Badge -> Audit another link
- Category cards use colored pill labels, no progress bars, 2-column grid on desktop
- Verdict shows as colored pill badge (GitHub-label style)
- Score pill displays numeric "X/10" format (e.g., "2/10" for safe, "8/10" for high) with severity color
- Copy link button functional next to verdict
- Badge image renders, markdown snippet copyable
- "Audit another skill" link navigates to /audit
</verification>

<success_criteria>
1. SEVERITY_CONFIG extended with numeric field mapping Score to 1-10 risk scale
2. Verdict displayed as large colored pill badge with numeric X/10 score pill and copy link in a row
3. Category cards in 2-column responsive grid with severity pill labels, no progress bars, expand/collapse working
4. Recommendation in separate card with Best for, Caveats, Alternatives
5. Utility and Badge sections restyled to design system cards
6. "Audit another skill" link at bottom navigates to /audit
7. All existing data (badge image, copy snippet, shareable URL, verdict, categories, utility) renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-audit-report-pages/09-02-SUMMARY.md`
</output>
