---
phase: 11-landing-page-sections
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/components/landing/__tests__/hero-section.test.tsx
  - apps/web/src/components/landing/__tests__/features-demo-section.test.tsx
  - apps/web/src/components/landing/__tests__/badge-snippet.test.tsx
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, FEAT-01, FEAT-02, FEAT-03, BADGE-01, BADGE-02, RESP-01, RESP-02]

must_haves:
  truths:
    - "Test stubs exist for all three landing section components"
    - "Each test file imports its target component and renders it"
    - "Tests cover all phase requirements as failing stubs"
  artifacts:
    - path: "apps/web/src/components/landing/__tests__/hero-section.test.tsx"
      provides: "Failing test stubs for HERO-01, HERO-02, HERO-03, RESP-02"
      contains: "describe"
    - path: "apps/web/src/components/landing/__tests__/features-demo-section.test.tsx"
      provides: "Failing test stubs for FEAT-01, FEAT-02, FEAT-03, RESP-01"
      contains: "describe"
    - path: "apps/web/src/components/landing/__tests__/badge-snippet.test.tsx"
      provides: "Failing test stubs for BADGE-01, BADGE-02"
      contains: "describe"
  key_links:
    - from: "apps/web/src/components/landing/__tests__/hero-section.test.tsx"
      to: "apps/web/src/components/landing/hero-section.tsx"
      via: "import"
      pattern: "import.*HeroSection"
    - from: "apps/web/src/components/landing/__tests__/features-demo-section.test.tsx"
      to: "apps/web/src/components/landing/features-demo-section.tsx"
      via: "import"
      pattern: "import.*FeaturesDemoSection"
    - from: "apps/web/src/components/landing/__tests__/badge-snippet.test.tsx"
      to: "apps/web/src/components/landing/badge-snippet.tsx"
      via: "import"
      pattern: "import.*BadgeSnippet"
---

<objective>
Create Wave 0 test stubs for all three landing page section components. These stubs define the expected behaviors that implementation plans 11-01 and 11-02 must satisfy.

Purpose: Establish the Nyquist verification contract before implementation begins. Tests will fail initially (RED) and pass after their respective implementation tasks complete (GREEN).
Output: Three test files with failing test stubs covering all phase requirements.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page-sections/11-CONTEXT.md
@.planning/phases/11-landing-page-sections/11-RESEARCH.md
@.planning/phases/11-landing-page-sections/11-VALIDATION.md

<interfaces>
<!-- Existing test setup -->
From apps/web/src/test-setup.ts:
```typescript
import "@testing-library/jest-dom/vitest";
```

<!-- Vitest config -->
From apps/web/vitest.config.ts:
```typescript
// environment: "jsdom", globals: true, setupFiles: ["./src/test-setup.ts"]
// Path alias: "@" -> src/
```

<!-- Current component exports that tests will import -->
From apps/web/src/components/landing/hero-section.tsx:
```typescript
export function HeroSection() // renders section with h1, two Link CTAs
```

From apps/web/src/components/landing/badge-snippet.tsx:
```typescript
export function BadgeSnippet() // renders section with ShieldBadge SVGs and CopyButton
```

<!-- New component that will be created by 11-02 -->
features-demo-section.tsx will export:
```typescript
export function FeaturesDemoSection() // two-column grid with feature cards + mock report demo
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero-section and badge-snippet test stubs</name>
  <files>apps/web/src/components/landing/__tests__/hero-section.test.tsx, apps/web/src/components/landing/__tests__/badge-snippet.test.tsx</files>
  <action>
Create the `__tests__` directory and two test files:

**hero-section.test.tsx** — Stubs for HERO-01, HERO-02, HERO-03, RESP-02:

```tsx
import { render, screen } from "@testing-library/react";
import { HeroSection } from "../hero-section";

// Mock next/link since we're in jsdom
vi.mock("next/link", () => ({
  default: ({ children, href, ...props }: any) => <a href={href} {...props}>{children}</a>,
}));

describe("HeroSection", () => {
  beforeEach(() => {
    render(<HeroSection />);
  });

  // HERO-01: Hero heading renders with correct text
  it("renders the heading 'Don't install blind.'", () => {
    const heading = screen.getByRole("heading", { level: 1 });
    expect(heading).toHaveTextContent("Don't install blind.");
  });

  // HERO-01: Heading has large font size style
  it("has heading with clamp font size for 120px desktop scaling", () => {
    const heading = screen.getByRole("heading", { level: 1 });
    expect(heading).toHaveStyle({ fontSize: expect.stringContaining("clamp") });
  });

  // HERO-02: Gradient orb element present with aria-hidden
  it("renders a gradient orb element with aria-hidden", () => {
    const orb = document.querySelector("[aria-hidden='true']");
    expect(orb).toBeInTheDocument();
    expect(orb).toHaveStyle({ filter: expect.stringContaining("blur") });
  });

  // HERO-03: Two CTA links with correct hrefs
  it("renders primary CTA linking to /audit", () => {
    const link = screen.getByRole("link", { name: /audit a skill/i });
    expect(link).toHaveAttribute("href", "/audit");
  });

  it("renders secondary CTA linking to /report/cursor-rules-architect", () => {
    const link = screen.getByRole("link", { name: /view example report/i });
    expect(link).toHaveAttribute("href", "/report/cursor-rules-architect");
  });

  // HERO-03: Primary CTA has rounded-full style matching header
  it("primary CTA has rounded-full class", () => {
    const link = screen.getByRole("link", { name: /audit a skill/i });
    expect(link.className).toContain("rounded-full");
  });

  // RESP-02: Heading has responsive sizing (clamp or breakpoint classes)
  it("heading style supports responsive scaling", () => {
    const heading = screen.getByRole("heading", { level: 1 });
    const style = heading.getAttribute("style") || "";
    // Should use clamp() for fluid scaling between mobile and desktop
    expect(style).toContain("clamp");
  });
});
```

**badge-snippet.test.tsx** — Stubs for BADGE-01, BADGE-02:

```tsx
import { render, screen } from "@testing-library/react";
import { BadgeSnippet } from "../badge-snippet";

describe("BadgeSnippet", () => {
  beforeEach(() => {
    render(<BadgeSnippet />);
  });

  // BADGE-01: Three ShieldBadge SVGs render
  it("renders three badge SVGs", () => {
    const badges = screen.getAllByRole("img");
    expect(badges).toHaveLength(3);
  });

  it("renders badge labels: Safe to Install, Use with Caution, Avoid", () => {
    expect(screen.getByLabelText(/safe to install/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/use with caution/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/avoid/i)).toBeInTheDocument();
  });

  // BADGE-02: Three CopyButton instances render (one per badge)
  it("renders three copy buttons (one per badge)", () => {
    const copyButtons = screen.getAllByRole("button", { name: /copy/i });
    expect(copyButtons).toHaveLength(3);
  });

  // BADGE-02: Each badge has its own markdown snippet
  it("renders three markdown snippet blocks", () => {
    const snippets = document.querySelectorAll("pre");
    expect(snippets).toHaveLength(3);
  });
});
```

These tests should fail against the current component implementations (RED state) because:
- HeroSection currently lacks clamp font sizing, gradient orb, and rounded-full CTA
- BadgeSnippet currently has only 1 CopyButton instead of 3
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate/apps/web && npx vitest run src/components/landing/__tests__/hero-section.test.tsx src/components/landing/__tests__/badge-snippet.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist in __tests__/ directory. Tests run (expected to fail against current implementations — RED state). Hero tests cover HERO-01, HERO-02, HERO-03, RESP-02. Badge tests cover BADGE-01, BADGE-02.</done>
</task>

<task type="auto">
  <name>Task 2: Create features-demo-section test stubs</name>
  <files>apps/web/src/components/landing/__tests__/features-demo-section.test.tsx</files>
  <action>
Create the test file for the features-demo-section component that will be created by plan 11-02. Since the component does not exist yet, this test file must be written so it will fail at import time until 11-02 creates the component.

**features-demo-section.test.tsx** — Stubs for FEAT-01, FEAT-02, FEAT-03, RESP-01:

```tsx
import { render, screen } from "@testing-library/react";
import { FeaturesDemoSection } from "../features-demo-section";

describe("FeaturesDemoSection", () => {
  beforeEach(() => {
    render(<FeaturesDemoSection />);
  });

  // FEAT-01: Three feature cards render with correct titles
  it("renders feature card titled 'AI Security Analysis'", () => {
    expect(screen.getByText("AI Security Analysis")).toBeInTheDocument();
  });

  it("renders feature card titled 'CLI Gate'", () => {
    expect(screen.getByText("CLI Gate")).toBeInTheDocument();
  });

  it("renders feature card titled 'Trust Badges'", () => {
    expect(screen.getByText("Trust Badges")).toBeInTheDocument();
  });

  // FEAT-01: Feature cards are NOT clickable links
  it("feature cards are not wrapped in links", () => {
    const aiCard = screen.getByText("AI Security Analysis").closest("a");
    expect(aiCard).toBeNull();
  });

  // FEAT-02: Mock demo shows "Use with Caution" verdict
  it("shows 'Use with Caution' verdict in mock demo", () => {
    expect(screen.getByText(/use with caution/i)).toBeInTheDocument();
  });

  // FEAT-02: Mock demo shows 6.2/10 score
  it("shows 6.2 score in mock demo", () => {
    expect(screen.getByText(/6\.2/)).toBeInTheDocument();
  });

  // FEAT-02: Mock demo has 5 category rows with pill severity labels
  it("renders 5 category names", () => {
    expect(screen.getByText("Hidden Logic")).toBeInTheDocument();
    expect(screen.getByText("Data Access")).toBeInTheDocument();
    expect(screen.getByText("Action Risk")).toBeInTheDocument();
    expect(screen.getByText("Permission Scope")).toBeInTheDocument();
    expect(screen.getByText("Override Attempts")).toBeInTheDocument();
  });

  it("renders severity pill labels", () => {
    // 2 Safe pills, 2 Low pills, 1 Moderate pill
    const safePills = screen.getAllByText("Safe");
    const lowPills = screen.getAllByText("Low");
    const moderatePills = screen.getAllByText("Moderate");
    expect(safePills).toHaveLength(2);
    expect(lowPills).toHaveLength(2);
    expect(moderatePills).toHaveLength(1);
  });

  // FEAT-03 / RESP-01: Two-column grid class present
  it("has responsive two-column grid layout", () => {
    const grid = document.querySelector(".lg\\:grid-cols-2");
    expect(grid).toBeInTheDocument();
  });

  // RESP-01: Default single column
  it("defaults to single column (grid-cols-1)", () => {
    const grid = document.querySelector(".grid-cols-1");
    expect(grid).toBeInTheDocument();
  });

  // FEAT-01: SVG icons present (ShieldIcon, TerminalIcon, BadgeIcon)
  it("renders three SVG icons for feature cards", () => {
    const section = document.querySelector("section");
    const svgs = section?.querySelectorAll("svg");
    expect(svgs?.length).toBeGreaterThanOrEqual(3);
  });
});
```

This test will fail at import time because `features-demo-section.tsx` does not exist yet. Plan 11-02 Task 1 creates the component, at which point these tests become the verification target.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate/apps/web && npx vitest run src/components/landing/__tests__/features-demo-section.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>features-demo-section.test.tsx exists with stubs for FEAT-01, FEAT-02, FEAT-03, RESP-01. Test file fails at import (RED state) because the component does not exist yet — expected behavior for Wave 0.</done>
</task>

</tasks>

<verification>
- All three test files exist in `apps/web/src/components/landing/__tests__/`
- `cd apps/web && npx vitest run src/components/landing/__tests__/ --reporter=verbose` runs all stubs (failures expected in RED state)
- Test stubs cover all 10 phase requirements: HERO-01, HERO-02, HERO-03, FEAT-01, FEAT-02, FEAT-03, BADGE-01, BADGE-02, RESP-01, RESP-02
</verification>

<success_criteria>
- Three test files created with describe/it blocks matching all phase requirements
- Tests are runnable by Vitest (may fail, but don't error on syntax)
- Hero test covers heading text, font sizing, gradient orb, CTA links, rounded-full style
- Features-demo test covers card titles, non-clickable cards, verdict, score, categories, pills, grid classes
- Badge test covers 3 SVGs, 3 copy buttons, 3 snippet blocks
</success_criteria>

<output>
After completion, create `.planning/phases/11-landing-page-sections/11-00-SUMMARY.md`
</output>
