---
phase: 11-landing-page-sections
plan: 01
type: execute
wave: 1
depends_on: ["11-00"]
files_modified:
  - apps/web/src/components/landing/hero-section.tsx
  - apps/web/src/components/landing/badge-snippet.tsx
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, BADGE-01, BADGE-02, RESP-02]

must_haves:
  truths:
    - "Hero heading 'Don't install blind.' renders at 120px on desktop, scaling down to ~64px on mobile"
    - "A violet gradient orb glows behind the hero heading without causing layout shift"
    - "Primary CTA 'Audit a skill' links to /audit with rounded-full violet button matching header style"
    - "Secondary CTA 'View example report' links to /report/cursor-rules-architect as a text link"
    - "Three badge variants (Safe to Install, Use with Caution, Avoid) each have their own copy-to-clipboard button"
    - "Badge markdown snippets each have an individual CopyButton instance"
  artifacts:
    - path: "apps/web/src/components/landing/hero-section.tsx"
      provides: "Hero section with 120px heading, gradient orb, two CTAs"
      contains: "Don't install blind"
    - path: "apps/web/src/components/landing/badge-snippet.tsx"
      provides: "Badge section with per-badge copy buttons"
      contains: "CopyButton"
  key_links:
    - from: "apps/web/src/components/landing/hero-section.tsx"
      to: "/audit"
      via: "Link href"
      pattern: "href=\"/audit\""
    - from: "apps/web/src/components/landing/hero-section.tsx"
      to: "/report/cursor-rules-architect"
      via: "Link href"
      pattern: "href=\"/report/cursor-rules-architect\""
    - from: "apps/web/src/components/landing/badge-snippet.tsx"
      to: "apps/web/src/components/copy-button.tsx"
      via: "CopyButton import"
      pattern: "import.*CopyButton"
---

<objective>
Rework the hero section with 120px heading, violet gradient orb, and restyled CTAs. Rework the badge section to give each badge variant its own individual copy-to-clipboard button.

Purpose: These two independent component reworks deliver the hero visual impact and improved badge UX for the v1.2 dark landing page.
Output: Updated hero-section.tsx and badge-snippet.tsx rendering correctly under the .dark-landing dark theme.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page-sections/11-CONTEXT.md
@.planning/phases/11-landing-page-sections/11-RESEARCH.md

<interfaces>
<!-- Existing CopyButton API the badge section will use -->

From apps/web/src/components/copy-button.tsx:
```typescript
export function CopyButton({
  text,
  label,
  className,
}: {
  text: string;
  label: string;
  className?: string;
}) // Returns button with copied state feedback
```

<!-- Existing header CTA button classes to match for hero primary CTA -->
Header "Try it" button classes:
```
bg-accent hover:bg-accent-hover text-white rounded-full px-4 py-1.5 font-semibold text-sm transition-colors
```
For hero, scale up: `rounded-full px-6 py-3 font-semibold` (same colors, larger padding).

<!-- Dark theme token classes available via .dark-landing -->
- text-text-heading (resolves to white on dark landing)
- text-text-body (resolves to light gray on dark landing)
- text-text-muted (resolves to muted on dark landing)
- bg-surface-card (resolves to #2d2640 on dark landing)
- bg-surface-2 (resolves to dark surface)
- border-border (resolves to dark border)
- bg-accent / hover:bg-accent-hover (violet #9d7aff)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework hero section with 120px heading, gradient orb, and restyled CTAs</name>
  <files>apps/web/src/components/landing/hero-section.tsx</files>
  <action>
Rewrite hero-section.tsx with these specific changes:

1. **Heading size**: Change the h1 from `text-4xl sm:text-5xl` to inline style `fontSize: 'clamp(48px, 10vw, 120px)'` with `lineHeight: 1.05`. Keep `font-bold tracking-tight text-text-heading`. Add period: "Don't install blind." (current is missing the period).

2. **Gradient orb**: Add a `relative overflow-hidden` to the section. Insert an absolutely-positioned div before the content:
   ```
   className="pointer-events-none absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2"
   style={{ width: '600px', height: '600px', background: 'radial-gradient(circle, rgba(157, 122, 255, 0.20) 0%, transparent 70%)', filter: 'blur(60px)' }}
   aria-hidden="true"
   ```
   Wrap heading+subtitle+CTAs in a `relative z-10` div so they render above the orb.

3. **Section sizing**: Change `max-w-3xl` to `max-w-4xl`. Change `py-20` to `pb-16 pt-32` for more top breathing room with the floating pill header above.

4. **Primary CTA**: Change `rounded-lg` to `rounded-full` to match header "Try it" button style. Keep `bg-accent px-6 py-3 font-semibold text-white transition-colors hover:bg-accent-hover`.

5. **Keep** the existing subtitle text and secondary CTA link exactly as they are.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate/apps/web && npx vitest run src/components/landing/__tests__/hero-section.test.tsx -x 2>&1 | tail -15</automated>
  </verify>
  <done>Hero section renders "Don't install blind." at 120px on desktop with fluid scaling down, violet gradient orb behind text (overflow-hidden, pointer-events-none, aria-hidden), primary CTA is rounded-full matching header style, secondary CTA links to example report. All hero-section.test.tsx tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rework badge section with per-badge copy buttons</name>
  <files>apps/web/src/components/landing/badge-snippet.tsx</files>
  <action>
Refactor badge-snippet.tsx to give each badge its own markdown snippet and copy button:

1. **Layout change**: Instead of badges in a row followed by a single shared snippet, render each badge as a vertical card: badge SVG on top, markdown snippet below with its own CopyButton.

2. **Container layout**: Use `grid grid-cols-1 gap-6 md:grid-cols-3` for the 3 badge cards. On mobile (<=768px) they stack vertically per user decision.

3. **Each badge card structure**:
   ```tsx
   <div className="flex flex-col items-center gap-3">
     <ShieldBadge {...variant} />
     <div className="relative w-full">
       <pre className="bg-surface-2 border border-border rounded-lg p-3 pr-20 font-mono text-xs text-text-body overflow-x-auto">
         {snippetText}
       </pre>
       <div className="absolute top-2 right-2">
         <CopyButton text={snippetText} label="Copy" />
       </div>
     </div>
   </div>
   ```

4. **Snippet text**: All badges use the same generic placeholder: `![Skillgate](https://skillgate.sh/badge/your-skill)` per user decision.

5. **Section heading**: Keep "Add a trust badge" heading. Keep the subtitle text about adding badges to README.

6. **Remove** the old single shared snippet `<pre>` block and its single CopyButton.

7. **Keep** the existing ShieldBadge component and badgeVariants array unchanged.

8. **Section container**: Use `max-w-4xl` (up from `max-w-2xl`) to accommodate the 3-column layout.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate/apps/web && npx vitest run src/components/landing/__tests__/badge-snippet.test.tsx -x 2>&1 | tail -15</automated>
  </verify>
  <done>Badge section shows 3 badge variants each with their own ShieldBadge SVG and individual CopyButton for the markdown snippet. Layout is 3-column on desktop, stacks vertically on mobile. All badge-snippet.test.tsx tests pass.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vitest run src/components/landing/__tests__/hero-section.test.tsx src/components/landing/__tests__/badge-snippet.test.tsx -x` — all tests green
- `cd apps/web && npx next build` completes without errors
- Landing page at localhost:3000 shows hero with large heading and gradient orb
- Landing page shows 3 badge cards each with their own copy button
- No horizontal scrollbar on mobile viewport (375px)
</verification>

<success_criteria>
- Hero heading "Don't install blind." renders at 120px on desktop, scales fluidly to ~48-64px on mobile
- Violet gradient orb visible behind hero text without layout shift
- Primary CTA is rounded-full violet button, secondary is text link
- Three badge variants each have individual copy-to-clipboard functionality
- All styling inherits correctly from .dark-landing token system
- All Vitest hero and badge tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-landing-page-sections/11-01-SUMMARY.md`
</output>
