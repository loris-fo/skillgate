---
phase: 13-universal-audit-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/audit-engine/src/prompt.ts
  - packages/audit-engine/src/types.ts
  - packages/audit-engine/src/schema.ts
  - packages/audit-engine/tests/schema.test.ts
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "Audit prompt addresses 'AI agent instruction files' not 'Claude Code SKILL.md files'"
    - "Audit prompt includes agent-specific risk pattern examples across all 6 agents (Claude, Cursor, Windsurf, Copilot, Cline, Aider)"
    - "AuditResult type includes optional detected_agent field with agent enum values"
    - "Existing audit results without detected_agent still pass Zod validation"
    - "AUDIT_TOOL schema includes detected_agent so Claude returns agent identification"
    - "Prompt instructs Claude to mention detected agent naturally in summary field"
  artifacts:
    - path: "packages/audit-engine/src/prompt.ts"
      provides: "Universal auditor system prompt with agent-specific risk patterns"
      contains: "AI agent instruction files"
    - path: "packages/audit-engine/src/types.ts"
      provides: "AuditResult with optional detected_agent field"
      contains: "detected_agent"
    - path: "packages/audit-engine/src/schema.ts"
      provides: "Zod schema and AUDIT_TOOL with detected_agent"
      contains: "detected_agent"
    - path: "packages/audit-engine/tests/schema.test.ts"
      provides: "Tests validating detected_agent optional behavior"
      contains: "detected_agent"
  key_links:
    - from: "packages/audit-engine/src/schema.ts"
      to: "packages/audit-engine/src/types.ts"
      via: "Zod schema mirrors AuditResult type including detected_agent"
      pattern: "detected_agent.*optional"
    - from: "packages/audit-engine/src/schema.ts"
      to: "packages/audit-engine/src/engine.ts"
      via: "AUDIT_TOOL and auditResultSchema consumed by engine"
      pattern: "AUDIT_TOOL|auditResultSchema"
---

<objective>
Update the audit engine to analyze any AI agent's instruction files with agent-specific risk patterns, and add a detected_agent field to the audit result schema.

Purpose: Enable SkillGate to audit skill/rule files from Claude, Cursor, Windsurf, Copilot, Cline, and Aider -- not just Claude SKILL.md files. This is the core engine change that downstream phases (scan, install, detection) depend on.

Output: Updated prompt.ts, types.ts, schema.ts, and schema tests -- all backward compatible with existing cached audits.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-universal-audit-engine/13-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/audit-engine/src/types.ts:
```typescript
export type Score = "safe" | "low" | "moderate" | "high" | "critical";
export type Verdict = "install" | "install_with_caution" | "review_first" | "avoid";
export type AuditResult = {
  overall_score: Score;
  verdict: string;
  summary: string;
  intent: string;
  categories: Categories;
  utility_analysis: UtilityAnalysis;
  recommendation: Recommendation;
};
```

From packages/audit-engine/src/schema.ts:
```typescript
export const auditResultSchema = z.object({ ... });  // Zod validation
export const AUDIT_TOOL: Anthropic.Tool = { ... };     // Anthropic tool definition
```

From packages/audit-engine/src/prompt.ts:
```typescript
export const SYSTEM_PROMPT = `You are a security auditor for Claude Code SKILL.md files...`;
export function buildUserMessage(skillContent: string): string { ... }
```

From packages/audit-engine/src/engine.ts:
```typescript
// Consumes SYSTEM_PROMPT, buildUserMessage, AUDIT_TOOL, auditResultSchema
// No changes needed in engine.ts -- it reads from the modules above
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detected_agent to type, schema, and tool definition</name>
  <files>packages/audit-engine/src/types.ts, packages/audit-engine/src/schema.ts, packages/audit-engine/tests/schema.test.ts</files>
  <behavior>
    - Test: existing mockAuditResult (without detected_agent) still passes validation
    - Test: audit result with detected_agent: "cursor" passes validation
    - Test: audit result with detected_agent: "unknown" passes validation
    - Test: audit result with invalid detected_agent value (e.g., "openai") is rejected
    - Test: audit result without detected_agent passes (field is optional)
  </behavior>
  <action>
    1. In types.ts: Add `DetectedAgent` type as `"claude" | "cursor" | "windsurf" | "copilot" | "cline" | "aider" | "unknown"`. Add optional `detected_agent?: DetectedAgent` field to `AuditResult`. Export `DetectedAgent` type.

    2. In schema.ts:
       - Add `detectedAgentEnum = z.enum(["claude", "cursor", "windsurf", "copilot", "cline", "aider", "unknown"])`
       - Add `detected_agent: detectedAgentEnum.optional()` to `auditResultSchema`
       - Add `detected_agent` property to `AUDIT_TOOL.input_schema.properties` with enum values matching the type
       - Do NOT add `detected_agent` to the `required` array in AUDIT_TOOL (it is optional)
       - Update AUDIT_TOOL description from "Record the security audit result for the analyzed SKILL.md" to "Record the security audit result for the analyzed instruction file"

    3. In index.ts: Export `DetectedAgent` type alongside other type exports.

    4. In tests/schema.test.ts:
       - Add test: existing mockAuditResult without detected_agent still validates
       - Add test: result with `detected_agent: "cursor"` validates
       - Add test: result with `detected_agent: "unknown"` validates
       - Add test: result with `detected_agent: "openai"` rejects (invalid enum value)
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && pnpm --filter @skillgate/audit-engine test -- --run</automated>
  </verify>
  <done>AuditResult type includes optional detected_agent, Zod schema validates it correctly, AUDIT_TOOL includes the field, all existing tests still pass, new tests cover optional/valid/invalid cases</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite prompt to universal auditor with agent-specific risk patterns</name>
  <files>packages/audit-engine/src/prompt.ts</files>
  <action>
    Rewrite SYSTEM_PROMPT and buildUserMessage in prompt.ts per these CONTEXT.md locked decisions:

    **SYSTEM_PROMPT changes:**

    1. Identity line: Change from "You are a security auditor for Claude Code SKILL.md files" to "You are a security auditor for AI agent instruction files" (per decision: universal auditor identity, no agent named)

    2. Replace all "SKILL.md" references with "instruction file" terminology (per decision: generic terminology)

    3. Keep the injection defense paragraph exactly as-is (the `<skill_content>` XML fence tag name stays unchanged per decision)

    4. Keep all 5 security categories with their existing descriptions, but enrich each with 2-3 agent-specific risk pattern examples (per decision: fold into existing categories as enriched examples). Use generic descriptions, not agent-attributed labels (per decision):
       - Hidden Logic: Add examples like "embedded editor configuration that silently changes settings", "encoded shell pipelines triggered by specific file patterns"
       - Data Access: Add examples like "reading IDE configuration files beyond stated scope", "accessing shell history or clipboard contents"
       - Action Risk: Add examples like "modifying editor settings or workspace configuration without explicit mention", "executing shell commands through indirect directive patterns"
       - Permission Scope: Add examples like "writing to directories outside the project workspace", "modifying global tool configuration files"
       - Override Attempts: Change "Claude's safety guidelines" to "AI agent safety constraints" (per decision). Add examples like "instructions to ignore previous system prompts", "directives to bypass tool confirmation dialogs"

    5. Add agent detection instruction at the end of the prompt (after recommendation section, before the closing sentence): "When you can identify which AI agent the instruction file is written for (based on content patterns, terminology, or directive style), mention it naturally in the summary field (e.g., 'This Cursor rules file instructs the agent to...'). If you cannot identify the agent, simply describe what the file does without guessing. Also set the detected_agent field to the appropriate value, or 'unknown' if uncertain." (per decisions: detect from content patterns, mention in summary naturally, skip if uncertain, no pattern hints list)

    6. Keep the closing sentence about using record_audit tool but update to reference "instruction file" instead of any specific format.

    **buildUserMessage changes:**

    1. Change opening from "Analyze the following SKILL.md file" to "Analyze the following AI agent instruction file"
    2. Keep `<skill_content>` XML tag name unchanged (per decision: internal detail)
    3. Keep the injection defense reminder unchanged
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && pnpm --filter @skillgate/audit-engine exec -- node -e "const p = require('./dist/prompt.js'); console.log(p.SYSTEM_PROMPT.includes('AI agent instruction files') && !p.SYSTEM_PROMPT.includes('Claude Code SKILL.md') && p.SYSTEM_PROMPT.includes('detected_agent') && p.buildUserMessage('test').includes('AI agent instruction file') ? 'PASS' : 'FAIL')" 2>/dev/null || (cd /Users/lorisfochesato/Dev/skillgate && pnpm --filter @skillgate/audit-engine build && pnpm --filter @skillgate/audit-engine exec -- node -e "const p = require('./dist/prompt.js'); console.log(p.SYSTEM_PROMPT.includes('AI agent instruction files') && !p.SYSTEM_PROMPT.includes('Claude Code SKILL.md') && p.SYSTEM_PROMPT.includes('detected_agent') && p.buildUserMessage('test').includes('AI agent instruction file') ? 'PASS' : 'FAIL')")</automated>
  </verify>
  <done>SYSTEM_PROMPT uses universal "AI agent instruction files" identity with no Claude-specific language. All 5 categories enriched with agent-specific risk pattern examples using generic descriptions. Override Attempts references "AI agent safety constraints" not "Claude's safety guidelines". Agent detection instructions included for summary field and detected_agent field. buildUserMessage uses "AI agent instruction file" terminology. The skill_content XML fence tag is unchanged.</done>
</task>

</tasks>

<verification>
1. All existing audit-engine tests pass (backward compatibility confirmed)
2. New schema tests validate detected_agent optional behavior
3. SYSTEM_PROMPT contains no "Claude Code SKILL.md" or "Claude's safety guidelines" references
4. SYSTEM_PROMPT contains "AI agent instruction files" and agent-specific risk pattern examples
5. AuditResult type includes optional detected_agent with correct enum values
6. AUDIT_TOOL schema includes detected_agent as optional field
7. Build succeeds: `pnpm --filter @skillgate/audit-engine build`
</verification>

<success_criteria>
- Submitting any agent's instruction file (Cursor rules, Windsurf config, Copilot instructions, etc.) would receive an agent-agnostic audit with relevant risk pattern analysis
- The audit prompt no longer assumes Claude-specific context
- detected_agent field exists in schema for downstream consumers (CLI scan, install)
- All existing cached audits remain valid (optional field, no cache invalidation)
</success_criteria>

<output>
After completion, create `.planning/phases/13-universal-audit-engine/13-01-SUMMARY.md`
</output>
