---
phase: 10-dark-design-tokens-layout-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03

must_haves:
  truths:
    - "Landing page has dark purple background (#1a1625) while /audit and /report pages show light sky-blue (#F0F9FF)"
    - "Severity colors render identically on both dark landing and light product pages"
    - ".dark-landing class is applied to body only on the landing page route"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "Dark token overrides under .dark-landing selector, fixed severity colors"
      contains: ".dark-landing"
    - path: "apps/web/src/app/layout.tsx"
      provides: "Conditional .dark-landing class on body, bg-bg-page moved to main"
      contains: "usePathname"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/app/globals.css"
      via: ".dark-landing class triggers CSS custom property overrides"
      pattern: "dark-landing"
---

<objective>
Add dark purple/violet design tokens scoped to landing page and wire layout plumbing so dark theme activates only on /.

Purpose: Foundation for all Phase 10 dark styling -- header, footer, and Phase 11 sections will inherit these tokens automatically.
Output: globals.css with .dark-landing overrides, layout.tsx with conditional body class and bg-bg-page on main.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dark-design-tokens-layout-foundation/10-CONTEXT.md

@apps/web/src/app/globals.css
@apps/web/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark design tokens and fix severity colors in globals.css</name>
  <files>apps/web/src/app/globals.css</files>
  <action>
In apps/web/src/app/globals.css, make two changes:

1. Fix severity color tokens in the existing @theme block:
   - --color-severity-low: #3B82F6 (change from #EAB308 yellow to blue)
   - --color-severity-moderate: #F59E0B (change from #F97316 orange to amber)
   - --color-severity-high remains #F97316 (unchanged)
   All other severity colors stay the same.

2. After the @theme block, add a .dark-landing selector that overrides the theme tokens with dark purple/violet values. Use regular CSS custom properties (not @theme) since these are scoped overrides:

```css
.dark-landing {
  --color-bg-page: #1a1625;
  --color-surface-card: #2d2640;
  --color-border-card: #3d3555;
  --color-border-card-hover: #9d7aff;
  --color-accent: #9d7aff;
  --color-accent-hover: #b49aff;
  --color-accent-muted: #9d7aff20;
  --color-text-heading: #ffffff;
  --color-text-body: #cbd5e1;
  --color-text-muted: #94a3b8;
  /* Backward-compat aliases */
  --color-surface-0: #1a1625;
  --color-surface-1: #2d2640;
  --color-surface-2: #231e30;
  --color-surface-3: #3d3555;
  --color-border: #3d3555;
  --color-text-primary: #ffffff;
  --color-text-secondary: #cbd5e1;
}
```

Do NOT override --color-severity-* values inside .dark-landing. Severity colors must stay static so they render identically on both themes.
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -c "dark-landing" apps/web/src/app/globals.css && grep "#3B82F6" apps/web/src/app/globals.css && grep "#F59E0B" apps/web/src/app/globals.css && echo "PASS"</automated>
  </verify>
  <done>globals.css contains .dark-landing block with all dark token overrides, severity-low is blue (#3B82F6), severity-moderate is amber (#F59E0B), no severity overrides inside .dark-landing</done>
</task>

<task type="auto">
  <name>Task 2: Wire conditional dark-landing class and move background to main in layout.tsx</name>
  <files>apps/web/src/app/layout.tsx</files>
  <action>
Convert layout.tsx so the body element conditionally gets the .dark-landing class on the landing page route only.

Since layout.tsx is a server component (renders metadata), create a small client wrapper component inline or in a separate file. The simplest approach: create a new client component `apps/web/src/components/layout-body.tsx` that:

1. Uses `"use client"` and `usePathname()` from `next/navigation`
2. Determines `isLanding = pathname === "/"`
3. Renders the `<body>` tag with:
   - Existing classes: font vars, `text-text-primary font-sans antialiased min-h-screen flex flex-col`
   - Conditional: `dark-landing` class when `isLanding` is true
   - Remove `bg-bg-page` from body (it moves to main)
4. Renders children inside

Then in layout.tsx:
- Import and use `LayoutBody` instead of a raw `<body>` tag
- Move `bg-bg-page` to the `<main>` element: `<main className="flex-1 bg-bg-page">`
- Pass the font variable className to LayoutBody so it can apply it to body

This way:
- Landing page body gets .dark-landing, main gets bg-bg-page which resolves to #1a1625
- Other pages: no .dark-landing on body, main gets bg-bg-page which resolves to #F0F9FF
- Layout.tsx stays a server component (for metadata export)
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -q "usePathname" apps/web/src/components/layout-body.tsx && grep -q "dark-landing" apps/web/src/components/layout-body.tsx && grep -q "bg-bg-page" apps/web/src/app/layout.tsx && npx turbo build --filter=web 2>&1 | tail -5</automated>
  </verify>
  <done>Landing page (/) renders with .dark-landing on body and dark purple background on main. /audit and /report pages render without .dark-landing and retain light sky-blue background. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- Visit / in dev server: background should be dark purple (#1a1625)
- Visit /audit: background should be light sky-blue (#F0F9FF)
- Severity badge colors should look identical on both pages
- No TypeScript or build errors
</verification>

<success_criteria>
- .dark-landing CSS class exists in globals.css with all dark token overrides
- Severity colors fixed: low=blue, moderate=amber
- Body element gets .dark-landing class only on / route
- bg-bg-page moved from body to main element
- Build passes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-design-tokens-layout-foundation/10-01-SUMMARY.md`
</output>
