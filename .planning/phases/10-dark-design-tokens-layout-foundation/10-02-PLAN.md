---
phase: 10-dark-design-tokens-layout-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - apps/web/src/components/header.tsx
  - apps/web/src/components/footer.tsx
autonomous: true
requirements:
  - HDR-01
  - HDR-02
  - HDR-03
  - FTR-01
  - FTR-02

must_haves:
  truths:
    - "Landing page header appears as a floating rounded pill with backdrop blur, showing shield icon, wordmark, nav links, CLI pill, and CTA button"
    - "Non-landing pages retain existing sticky header with border-bottom, wordmark, npm pill, and GitHub icon"
    - "Landing page footer renders with dark text and purple top border"
    - "Non-landing pages retain existing footer styling unchanged"
  artifacts:
    - path: "apps/web/src/components/header.tsx"
      provides: "Conditional floating pill (landing) vs sticky bar (other pages)"
      contains: "usePathname"
    - path: "apps/web/src/components/footer.tsx"
      provides: "Dark variant footer on landing page"
      contains: "usePathname"
  key_links:
    - from: "apps/web/src/components/header.tsx"
      to: "next/navigation"
      via: "usePathname() determines isLanding for conditional rendering"
      pattern: "usePathname"
    - from: "apps/web/src/components/footer.tsx"
      to: "next/navigation"
      via: "usePathname() determines isLanding for dark variant styling"
      pattern: "usePathname"
---

<objective>
Implement conditional header (floating pill on landing, sticky bar elsewhere) and dark footer variant for landing page.

Purpose: Complete the layout foundation so landing page has its distinct dark marketing header and footer while product pages remain untouched.
Output: Header with two visual modes, Footer with dark landing variant.
</objective>

<execution_context>
@/Users/lorisfochesato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorisfochesato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dark-design-tokens-layout-foundation/10-CONTEXT.md
@.planning/phases/10-dark-design-tokens-layout-foundation/10-01-SUMMARY.md

@apps/web/src/components/header.tsx
@apps/web/src/components/footer.tsx
</context>

<interfaces>
<!-- From 10-01: layout-body.tsx provides the .dark-landing class on body for landing page -->
<!-- Dark tokens from globals.css are active when .dark-landing is on body -->
<!-- Token values auto-resolve: --color-text-heading → white, --color-accent → #9d7aff on landing -->

From apps/web/src/app/globals.css (after Plan 01):
- .dark-landing overrides: bg-page=#1a1625, surface-card=#2d2640, accent=#9d7aff, text-heading=white, text-body=#cbd5e1
- Components using token-based Tailwind classes (text-text-heading, bg-surface-card, etc.) automatically get dark values on landing
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement conditional floating pill header</name>
  <files>apps/web/src/components/header.tsx</files>
  <action>
Rewrite header.tsx to conditionally render two different visual modes using `usePathname()` from `next/navigation`. The component already has `"use client"`.

**When `pathname === "/"` (landing):**
- Position: `fixed top-4 left-1/2 -translate-x-1/2 z-50` (floating, centered)
- Shape: `rounded-full` pill with `backdrop-blur-md bg-white/5 border border-white/10`
- Width: constrained with `max-w-4xl w-[calc(100%-2rem)]` (or similar, use discretion for good proportions)
- Content (left to right):
  - Shield icon (inline SVG, simple shield shape) + "skillgate" wordmark text (font-bold, text-white via text-text-heading)
  - Nav links: "Docs" (href="https://github.com/lorisfochesato/skillgate#readme"), "GitHub" (href="https://github.com/lorisfochesato/skillgate") — text-text-body with hover:text-white transition
  - CLI pill: `npx skillgate` with copy-to-clipboard (reuse existing handleCopy pattern but change text to "npx skillgate")
  - "Try it" CTA button: `bg-accent hover:bg-accent-hover text-white rounded-full px-4 py-1.5 font-semibold text-sm` linking to /audit
- Padding: `px-4 sm:px-6 py-2.5`

**When pathname !== "/" (other pages):**
- Keep EXACTLY the current header markup unchanged: sticky top-0, border-b, wordmark, "npm i -g skillgate" pill, GitHub icon
- No visual changes whatsoever to non-landing header

Use a simple `if (isLanding) { return ... } else { return ... }` pattern for clarity. Both branches share the `copied`/`handleCopy` state but with different copy text ("npx skillgate" on landing, "npm i -g skillgate" on other pages).

Shield icon SVG (simple, ~24x24):
```
<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
  <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
</svg>
```
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -q "usePathname" apps/web/src/components/header.tsx && grep -q "rounded-full" apps/web/src/components/header.tsx && grep -q "npx skillgate" apps/web/src/components/header.tsx && grep -q "Try it" apps/web/src/components/header.tsx && grep -q "sticky top-0" apps/web/src/components/header.tsx && npx turbo build --filter=web 2>&1 | tail -5</automated>
  </verify>
  <done>Header shows floating pill with shield icon, wordmark, nav links, CLI pill ("npx skillgate"), and "Try it" CTA on landing page. Other pages show existing sticky header unchanged. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add dark footer variant for landing page</name>
  <files>apps/web/src/components/footer.tsx</files>
  <action>
Convert footer.tsx to a client component with conditional dark styling on landing.

1. Add `"use client"` directive at top
2. Import `usePathname` from `next/navigation`
3. Determine `isLanding = usePathname() === "/"`

**When landing (isLanding = true):**
- Add a purple top border: `border-t border-[#9d7aff]/30` (or border-accent/30)
- Text uses dark theme values: `text-text-body` (resolves to #cbd5e1 under .dark-landing)
- Link hover: `hover:text-text-heading` (resolves to white under .dark-landing)
- Same links array, same layout — only color/border changes

**When not landing:**
- Keep EXACTLY current styling: `py-8`, `text-text-muted`, `hover:text-text-body`
- No border-t, no visual changes

Implementation: Use conditional className strings based on `isLanding`. The simplest approach:
```tsx
<footer className={`py-8 ${isLanding ? "border-t border-accent/30" : ""}`}>
  <div className="max-w-6xl mx-auto px-4 sm:px-6 text-center text-sm {isLanding ? 'text-text-body' : 'text-text-muted'}">
```

Keep the links array outside the component (no change needed there).
  </action>
  <verify>
    <automated>cd /Users/lorisfochesato/Dev/skillgate && grep -q '"use client"' apps/web/src/components/footer.tsx && grep -q "usePathname" apps/web/src/components/footer.tsx && grep -q "border-t" apps/web/src/components/footer.tsx && npx turbo build --filter=web 2>&1 | tail -5</automated>
  </verify>
  <done>Footer shows purple top border and light text on landing page, retains existing unstyled appearance on /audit and /report pages. Build passes.</done>
</task>

</tasks>

<verification>
- Visit / in dev server: header is floating pill with shield icon, wordmark, Docs/GitHub links, "npx skillgate" pill, "Try it" CTA
- Visit /audit: header is sticky bar with "skillgate" wordmark, "npm i -g skillgate" pill, GitHub icon (no visual change from before)
- Visit /: footer has purple top border and light text
- Visit /audit: footer looks exactly as before (no border, muted text)
- No TypeScript or build errors
</verification>

<success_criteria>
- Header conditionally renders floating pill (landing) vs sticky bar (other pages)
- Landing header contains: shield icon, wordmark, Docs link, GitHub link, "npx skillgate" copy pill, "Try it" CTA
- Non-landing header is visually identical to pre-Phase-10
- Footer renders dark variant on landing (purple border, light text)
- Non-landing footer is visually identical to pre-Phase-10
- Build passes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-design-tokens-layout-foundation/10-02-SUMMARY.md`
</output>
