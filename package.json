{
  "name": "skillgate",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @skillgate/audit-engine build && pnpm --filter @skillgate/web build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}